ANONYMIZED DELPHI FEEDBACK -- you are now producing your Round 3 revision.

There are no fixed (pre-included) items.

POOLED CANDIDATE ITEMS (open slots; the best <=8 make the final set).
  incl = how many of the 3 panelists currently include it (3=unanimous). [IN] = currently within the set; [cut] = below the line.

  K11 [IN]  incl=3 | Under congenial proper MI with an estimated scale and complete-data MLE target, the averaged imputation log-likelihood overstates the complete-data log-likelihood by +1/2 tr(RIV) plus a MAR design-imbalance term that vanishes under MCAR. | category: theory
  K16 [IN]  incl=3 | Applied teams evaluating AI-assisted statistical derivations should require auditable evidence of the same kind used here: sourced citations, independent symbolic checks, preregistered numerical criteria with reported failures, adversarial review, and reproducibility records. | category: applied-implication
  K14 [IN]  incl=3 | For nested MI likelihood-ratio comparisons at the null, the numerator's differential bias is tr(RIV_perp), the missing information in the tested directions projected in the complete-data information metric, not tr(RIV_full)-tr(RIV_0). | category: LRT-application
  K12 [IN]  incl=3 | The MI information-criterion bias is model-specific: candidate models can have different RIV traces, so adding tr(RIV_k) supplies the theoretical centering correction for MI-AIC and MI-BIC under the stated target. | category: IC-application
  K09 [IN]  incl=2 | Applied MI model-selection tables should add a per-candidate tr(RIV_k) correction to AIC/BIC when the target is complete-data-equivalent selection, because uncorrected criteria tilt toward models with more missing information. | category: applied-implication
  K03 [IN]  incl=2 | For information-criterion selection there is no protective reference distribution, so the per-model correction tr(RIV_k) must be applied to restore complete-data-equivalent ranking; for a calibrated likelihood-ratio test (Chan 2022) the mean is already absorbed and the correction must not be applied a second time. | category: applied-implication
  K10 [IN]  incl=2 | We make the structured AI-human workflow a first-class contribution by documenting, for this study, transparent provenance and auditable gates for sourced citations, human acceptance, dual computer-algebra verification, preregistered simulations, adversarial rederivation, and full reproducibility. | category: methodology-workflow
  K17 [IN]  incl=1 | Uncorrected multiple-imputation information criteria systematically favor models with more missing information, a bias correctable by a simple per-candidate penalty. | category: applied-implication
  K13 [cut] incl=1 | Centering the MI likelihood statistic and calibrating its reference distribution solve different problems, so the trace correction should be used when the statistic is used directly and not stacked on a Chan-calibrated MI LRT at the null. | category: applied-implication
  K04 [cut] incl=1 | We demonstrate a structured, ORCID-verified AI-human workflow whose every load-bearing claim passes derive/dual-CAS/Monte-Carlo gates with preregistration and cross-model adversarial review, and show this discipline catches errors single-lineage derivation would ship, such as eight of nine blind re-derivations returning the opposite sign of the main theorem. | category: methodology-workflow
  K07 [cut] incl=1 | We disclose two structural limits of the correction: the equal-fractions candidate family hides the over-statement of the naive trace difference, and per-model marginal transforms cannot calibrate a difference distribution. | category: applied-implication
  K08 [cut] incl=1 | The bias is firm only with an estimated scale and collapses to zero or minus one half the trace under known scale, so the correction should be applied only to estimated-scale fits. | category: applied-implication
  K15 [cut] incl=1 | Nested MI likelihood-ratio comparisons should reuse the same imputations for the competing fits, since pairing reduces null and local-alternative Monte Carlo noise from order sqrt(n) to order one. | category: LRT-application

CONTESTED ITEMS (not unanimous) -- dissent rationales by anonymous panelist:
  K03 (incl=2): B keeps -- "High-value boundary guidance: where to apply the correction and where double-correction inflates size (W2)."; A did NOT include; C keeps -- "Crucial boundary-condition guidance to prevent practitioners from double-counting the correction."
  K09 (incl=2): B did NOT include; A keeps -- "Adopted over K01/K17 because it is the clearest skim-level practice change and is scoped to AIC/BIC under the complete-data-equivalent target."; C keeps -- "Provides the most urgent practical takeaway for applied researchers as the primary skim hook."
  K10 (incl=2): B keeps -- "Workflow as first-class contribution, scoped 'for this study' to respect the no-second-coder limit; the journal's stated skim hook."; A keeps -- "Adopted as the first-class workflow contribution, scoped to this study's verified provenance and gates rather than a general guarantee."; C did NOT include
  K04 (incl=1): B did NOT include; A did NOT include; C keeps -- "Establishes the workflow as a first-class contribution by highlighting mechanically verified evidence and the caught sign error."
  K07 (incl=1): B did NOT include; A did NOT include; C keeps -- "Provides honest scoping by disclosing the structural limits of the correction, preventing over-generalization."
  K08 (incl=1): B keeps -- "Scoping guardrail (estimated-scale only) prevents misapplication; firm per the brief's known-scale collapse."; A did NOT include; C did NOT include
  K13 (incl=1): B did NOT include; A keeps -- "Adopted over K03/K19 because it cleanly separates centering from Chan-style calibration and scopes the no-double-counting warning to null calibrated MI LRTs."; C did NOT include
  K15 (incl=1): B did NOT include; A keeps -- "Adopted as a practical LRT consequence, scoped to the proven null and local-alternative Monte Carlo noise reduction."; C did NOT include
  K17 (incl=1): B keeps -- "Most urgent applied takeaway, clearest single-read phrasing of the IC tilt and the computable fix."; A did NOT include; C did NOT include

YOUR TASK: revise YOUR own set. You may adopt a pooled item (cite its K-id), drop one you previously held, or add a brand-new item. Re-rank for priority. Give a one-line reason for every change (or 'unchanged').

Hard rules, applied on EVERY round: (1) The final set is at most 8 items. (2) The set must cover all of: theory, LRT-application, IC-application, methodology-workflow, and at least two applied-implication items. (3) Every claim must be scoped to exactly what the brief's stated evidence and trust tier support; if you think a candidate as written overstates its evidence, reframe it to what the brief actually supports and say so in the reason. (4) No claim may assert a number, result, or fact that is not present in the brief. (5) Prefer phrasing that an applied researcher with quantitative training and working knowledge of multiple imputation understands on a single read; avoid house jargon.

Return ONLY a single JSON object (no markdown fences, no prose outside it):
{
  "keep": [ { "id": "K03", "priority": 1, "reason": "<one line; why kept or why the rank moved, or 'unchanged'>" } ],
  "add": [ { "title": "<title>", "by": "<attribution>", "category": "<category>", "reason": "<one line>", "priority": 9 } ],
  "drop_notes": "<one line on anything you deliberately dropped vs last round, or 'none'>"
}
"keep" lists the pooled items (by their K-id) you include in YOUR set -- the fixed items are automatic, do NOT list them. "add" is OPTIONAL brand-new items not in the pool (use [] if none). IMPORTANT: if an item already has a K-id, reference it in "keep" -- do NOT re-enter it under "add". To keep your selection unchanged, list the same K-ids again.
