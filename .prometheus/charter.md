---
schema_version: 1
project_id: d51ab9db-0748-44c5-ad94-2eee09e126f5
project_name: mi-spectral
status: active
current_milestone: "Respond to the v1 AI review (paper 88): empirical reruns for the two cheap gaps (data-augmentation replication + MI-BIC) + prose/response-letter for the rest; non-normal via brms gated on a runtime spike; fold in the pending per-paragraph annotation + compile-enablement toward JAIGP submission."
last_synced_commit: 9febb02
tree_state: dirty
updated: "2026-07-02T22:05:54Z"
updated_by: wrap
cockpit: { provider: null, list_id: null }
supermemory_container: proj-d51ab9db-0748-44c5-ad94-2eee09e126f5
---









# Goal

A finite-M spectral correction for likelihood-based hypothesis tests under *congenial* multiple imputation (MI), with applications to likelihood-ratio model comparison and information-criterion model selection. Core result: the MI Q-function carries a deviance bias that decomposes as +1/2 * tr(RIV) (RIV = relative-increase-in-variance). It is a human-prompted AI derivation with transparent, ORCID-verified provenance, targeting JAIGP; every load-bearing claim is meant to be paired with a preregistered numerical witness. Predicted empirical signature: uncorrected MI-AIC misclassifications should concentrate on the highest-RIV candidate model -- confirmed (W3-C = 1.000) and adversarially reviewed.

# Current milestone

**Respond to the v1 AI review (paper 88).** First external AI review (Reviewer3.com; `manuscript/reviews/v1/ai-review-paper-88.md`) -- 15 comments across 5 personas. Triaged against the ACTUAL text (most "gaps" are already in the paper -- the reviewers under-read it): **Tier A (already in the paper -> response-letter rebuttal, no new work):** the data-augmentation control r1#8 (disclosed line 408, bounded 2-3% in G3, named future work line 504); the nested-confound r1#9 (W4 non-nested + MCAR twins already isolate mechanism from complexity); the heavy-tail N=1000 miss r1#7 (reviewer conflated N with the MC repetition count R -- one clarifying sentence). **Tier B (the one real decision):** BIC r3#1 is in the title (D-19) but never formulated in the body -- earn it or drop it. **Tier C (easy fixes via level3 -> rebuild):** abstract estimated-scale qualifier (proof#10) + "trace component" scoping (proof#11); define "+/-" (r2#4); name all four selection-sweep models (r2#5); explicit Sigma / correlation placement (r2#6); alpha "negative gradient" sign (contra#12); KS 0.019-vs-0.018 mismatch (contra#13); disambiguate k (contra#14); standardize m vs M (contra#15). **Tier D (modest new content):** non-normal/GLM behavior r3#2 (MVN is a scope decision -> Discussion prediction); congeniality diagnosis r3#3 has no cited method (-> todo/003). Marcus's steer: get EVIDENCE for the two cheap gaps (DA, BIC), not only rebut; "leave space" -- the forks are gates, not pre-solved.

_This session (2026-07-01) -- Phase 0 EXECUTED, and it turned the DA gap into a substantive finding that PAUSES Phase 1._ Wrote `todo/040` (registration, doc-before-code) + built the hand-rolled conjugate DA imputer (`verification/da-conjugate-engine.R`; R's own RNG, no `norm::da.norm` dependency) + ran the two smoke spikes and a 100x powered confirmation + a discriminating test. **Result:** exact Bayesian posterior-draw MI carries a *persistent* O(tr(RIV)) deviance discount vs the FITTED/+1/2 headline -- Jeffreys IW (my conjugate == Schafer `norm::da.norm`, identical to the 3rd decimal, -0.783 at N=200 -> -0.74 asymptote), Gaussian/Laplace -0.11 (a persistent *Jensen* term), brms -1.36 -- while **Amelia's bootstrap EMB (the engine the paper actually validated with) CONVERGES to +1/2 as N->inf** (-0.099 -> -0.019, ~O(1/N)). Confirmed to R=20000; engine controls all pass (I-step exact = FITTED to -0.04+/-0.17; theta* dispersion == I_obs^-1; PSD/RNG clean across 20 workers). Decomposition of -0.74: removable Sigma-mean inflation (-0.54, ~69%) + IW skew (-0.12) + persistent Jensen (-0.11, present even in the correct MLE-centered Laplace draw). Applied model-selection direction UNHARMED (term scales with each model's tr(RIV), reinforcing the high-RIV-favoring bias). Drafted a cross-model derivation handoff + prompt for **Fable** (`todo/040-cross-model-{handoff,prompt}.md`; Q1 IW coefficient / Q1b Jensen coefficient / Q2 the bootstrap-converges anomaly / Q3-Q4 scope+action). **Phase 1 is PAUSED pending Fable's verdict + Marcus's framing call.** Committed on branch `da-engine-phase0-todo040` (91da4df, off main @ 07f5cfd, unpushed; frozen originals untouched).

_This session (2026-07-02) -- the GPT-5.5 Pro gate ran and was triaged; the framing is revised, not yet landed._ Built, ahead of the gate: `verification/cas-wolfram/verify_engine_gap_sympy.py` (10/10 checks pass -- the four algebraic lemmas behind Fable's master formula in a concrete p=3 case, plus a p=1 closed-form engine table reconstructed independently that matches the gate package's own withheld grading key exactly: Jeffreys -1, Laplace -1/4, bootstrap 0); `verification/_modules/engine-gap-audit.R` (the spike promoted to cached-RDS + summary.csv, not yet wired into run_all.R); `todo/040-derivation-amendment-draft.md` (the exact proposed derivation.qmd text, staged not landed). Extended `run_layer3_openai.py` with Responses-API support for -pro models (gpt-5.5-pro 404s "not a chat model" on Chat Completions; submitted background=true + polled rather than one blocking multi-hour socket read); confirmed live against the API that xhigh, not "max", is the reasoning-effort ceiling (400 invalid_value lists the valid set). Ran the GPT-5.5 Pro xhigh gate end to end (`todo/040b-gpt55-gate-package.md`): Mode A (blind) independently reproduced the core mechanism (Schur concavity, E2≈7×E1 via a Sigma-centering constant of 0/(p+2)/-1, E3->0 exactly); Mode B (red-team) sustained C1/C2/C4/C5, escalated C3 (wording)/C6/C7, refuted C9 as literally stated, and delivered a genuine closed-form IMB derivation for C8 (Tallis truncated-bivariate-normal moments) -- closes that open question as a bonus, pending independent verification. Triaged both escalations against the package's own policy. **C9** (scope condition): Mode B's three counterexamples were valid -- revised the staged callout to require Xi=o(1/n) AND the imbalance term controlled, for a regular draw law, stated as sufficient rather than a bare iff. **C6** (the -0.244 "deflated arm" number): solid, independently reproduced at -0.2399 (se .0075) via the exact-identity evaluator, but the linear-rescaling mechanism both Mode B and Fable's own writeup assumed is wrong -- `diag-nscaling.R`'s deflation acts inside an iterative data-augmentation P-step, not a one-shot rescale, and feeds back to a materially smaller bias than linear scaling predicts (measured b_Sigma contraction -0.1010 vs -0.343 predicted); Fable's "83% centering / 13% mu-wander / 4% imbalance" percentage breakdown rests on that flawed reading and should NOT enter the manuscript (nothing already staged references it -- only the matrix-recentred -0.150 and full Jeffreys -0.78 values, both unaffected). C7 (bootstrap floor "<=0.01") remains open, flagged not resolved. Committed `f4900ec` (6 files: the CAS check, the module, the staged draft, the runner extension, both gate response files). Memory: `project_da_engine_finding` updated same session.

_This session (2026-07-02, continued) -- Marcus ratified; the amendment is LANDED; provenance verify-reads for the four already-owned papers are DONE._ After reviewing three Consensus.app due-diligence queries (no contradicting or preempting literature found; one new candidate surfaced -- a von Hippel small-sample companion paper, logged to `todo/003`, committed `0da1e52`), Marcus ratified the staged framing. Landed the three `derivation.qmd` edits exactly as staged (D9 forward-pointer :391-394; the fitted-vs-oracle callout corrected + new scope-and-correction callout inserted :422-479; the known-scale callout addition :812-816) plus `decisions.md` D-22, verified by a full `quarto render` (exit 0, all 20 Appendix B cells execute cleanly). Committed `2ed3f3a`. Marcus then asked whether full RAG/citation-discipline diligence was done: confirmed the landed text has ZERO external citations (nothing to check -- verified by grepping the actual commit diff, not assumed), confirmed litrev's vector-RAG/MotherDuck is DOWN (live-checked, same outage as todo/039), and fell back to direct file-reads over the owned corpus -- all four already-acquired papers Fable's audit leaned on (Wang-Robins 1998, Nielsen 2003, Schafer 1997 :3502-3532, Honaker 2011) were re-read and CONFIRMED to support their attributed claims verbatim (type-A/B distinction; properness-makes-Rubin's-rule-unbiased; the IW mean formula + m>=p+2 condition; the EMB algorithm description). **The DA-engine thread (todo/040/040b/041) is now CLOSED** except for one Marcus-only action: adding von Hippel-Bartlett 2021 + Efron 1979 + the new von Hippel small-sample paper to Zotero (cannot be done from here; acquisition -> lit-sync -> cite follows once he does).

_This session (2026-07-01/02) -- the FABLE AUDIT RAN IN-REPO AND RESOLVED THE FORK._ Fable 5 (max effort) answered Q1-Q4 with algebra + a new exact-identity evaluator (`verification/spikes/audit-engine-formula.R`: computes each engine's gap from its PARAMETER DRAWS alone, no imputation). **Verdict: option (i), theorem intact when scoped** -- the engine gap is D = -1/2 tr(RIV_mumu) - 1/2 sum_i tr(Vhat_i^-1 C_i b_Sigma C_i') - IMB, so the +1/2 headline holds for *second-moment-matched* draws (Xi = b_Sigma + Var(mu*) ~ 0: FITTED, bootstrap/EMB = Amelia), while every Bayesian posterior draw carries that characterized, prior-dependent discount. Mechanism CORRECTIONS to the handoff: no Schur-Jensen inflation (the Schur complement is matrix-CONCAVE; its curvature cancels the Sigma-mean-wander, exactly under MCAR) -- the persistent Laplace term is the MU-WANDER -1/2 tr(RIV_mumu) = -0.1011 (mu-only arm matches to 4 decimals; it is the known-scale C_n's mu-block surviving, so the estimated-scale "proper = FIML alike" sentence contradicted the paper's own boundary case); no persistent IW-shape term (the deflated arm's -0.244 was the anisotropic residual of the SCALAR deflation; matrix recentring collapses IW onto Laplace); the bootstrap escapes via the exact identity E*[Sigma~ + (mu~-muhat)^{x2}] = Sigmahat (measured +0.071 vs -0.114); NEW small MAR-only IMB ~ -0.006 tr(RIV) (exactly 0 under MCAR), carried by every drawing engine incl. Amelia. All three branches reproduced by the identity (IW -0.787/-0.783, Laplace -0.124/-0.121, boot+EM -0.088/Amelia -0.087; Laplace asymptote -0.107 = the handoff's -0.106). Provenance accounting done: corpus already holds Wang-Robins 1998 + Nielsen 2003 + Schafer 1997 (IW mean at :3502-3532) + Honaker (todo/003 row was stale, corrected); genuinely missing = von Hippel-Bartlett 2021 + Efron 1979 (+ conditional brms trio), all logged in todo/003. Committed cc62ad7. **Now waiting on Marcus's framing call; the in-repo provenance apparatus (GPT-5.5 gate, CAS, spike promotion, [mc]) is the bigger remaining lift.**

_This session (2026-07-02, continued further) -- provenance acquisition mechanics executed; the DA-engine thread has nothing left but the read-verify step._ Marcus added the three remaining references (von Hippel-Bartlett 2021, Efron 1979, the von Hippel small-sample paper) to Zotero and re-exported BBT. Ran `/lit-sync`: Efron synced cleanly on the first pass; both von Hippel papers were SKIPPED ("PDF not found in MI-SPECTRAL/") despite Marcus confirming the PDFs were physically in the Drive folder. Root-caused: the Drive-folder auto-namer drops the second word of a multi-word surname, so "von Hippel" became just `von_..._year.pdf` with "hippel" missing entirely, and `lit_sync.py`'s surname-substring matcher can't find a filename that doesn't contain the surname. Renamed both PDFs to their exact BBT citekeys (the working fallback convention already used for `Meng01121991.pdf`/`Tanner01061987.pdf`/`Tierney01031986.pdf` in that folder) and re-ran `/lit-sync` -- both wrote cleanly on the second pass (SHA256-keyed Mathpix cache hit, unaffected by the rename). All three now in `literature/`: `vonhippelMaximumLikelihoodMultiple2021.md`, `efronBootstrapMethodsAnother1979.md`, `vonhippelNewConfidenceIntervals2016.md`. Confirmed the small-sample paper's year as 2016 (*Structural Equation Modeling* 23(3):422-437, DOI `10.1080/10705511.2015.1047931`) via Zotero metadata, resolving the ambiguity flagged in todo/003. Promoted all three rows `proposed -> acquired` in `todo/003`. Drafted (not yet run) a self-contained verification prompt for a fresh session/agent to read all three in full and check their attributed claims against text -- recommended at Sonnet 5 / effort high (a reading-and-claim-matching task, not a derivation, so the cross-model xhigh protocol doesn't apply). Uncommitted at end of session: `.prometheus/charter.md`, `todo/003-references-to-acquire.md`, `literature/mi-spectral-better-bibTeX.bib` (refreshed export), plus the three new `literature/*.md` files.

# Milestones

The arc from start to the Goal. Mark each: `[x]` done - `[~]` current - `[ ]` upcoming.

- [x] Theory & derivation -- the +1/2*tr(RIV) deviance-bias decomposition (reframed after Chan 2022, AoS)
- [x] Preregistration of witness hypotheses (W1 theorem - W2 LRT power - W3 model selection)
- [x] Empirical validation runs executed (comprehensive sweep; W4 non-nested)
- [x] Proof of correctness -- formal propositions + CAS verification
- [x] Socratic review of the simulation evidence (S5) -- cross-family tribunal; verdict AMEND, no result retracted
- [x] Prose readability pass (S1-S6) -- 67/82 paragraphs rewritten, corpus FK 11.9 -> 10.1; T-08 closed; G6/G7 resolved (merged to main)
- [x] Rendering polish + AI-collaboration restructure (todo/034) -- equation line-breaks, figure legibility, arXiv template, new S1B section (D-12); MERGED to main (FF, f666dc2)
- [x] Contributions + applied-implications + abstract, Delphi-vetted (todo/035) -- Intro Contributions list (S1-P6b) + Discussion implications (S6-P7b) + tracked applied-first abstract; pool-then-axes Delphi (8/8 unanimous; 5/7/0); four-author title page (D-16); direct quotes paraphrased (D-17)
- [x] Prose-quality passes: vocab softening (corpus_vocab_check), accuracy/overclaim audit (overclaim_flag + adversarial workflow, 15 fixes @ 3d0d844), punctuation budget v2 (99eca65), cadence/style pass (cadence_flag + per-paragraph rewrite, 45 paragraphs, connectives 0.07 -> 0.16/sentence)
- [x] todo/036 tightening pass -- newline-structure guard (83e08ac) + fluff_flag.py scout (7bed69e) built; per-paragraph human-gated method (single Agent-tool adversary, NOT a Workflow) validated. Finding: corpus already lexically tight (10/85 paragraphs, 1 hit each, top hits load-bearing) -- full sweep SHELVED as not-worth-the-risk (Marcus); curated-5 mini-pass optional
- [x] apaquarto APA-manuscript preview + JAIGP front matter (2026-06-28) -- T5 title (D-19), two-class author block AI Authors/Prompters with ORCID + full affiliation (D-18 amends D-16), numbered sections; LaTeX gotchas solved (tinytex 2026, \openbox, \displayauthors override); preview-only, port at compile-enablement
- [x] todo/037 precision & accuracy restoration prose pass -- 4 reviewer-grade patterns (specificity, orphaned-'it', mechanism accuracy, color commentary) ALL resolved; 3 scouts + 2 check_plan guards + 2 CLAUDE.md standards built; per-paragraph human-gated single-adversary; 5 commits 15e1b1b..eb6c155; check_plan green; color+mechanism scouts clean corpus-wide; APA preview clean (85 ¶)
- [x] todo/038 text-validity defense -- **stage 1a (claim-support) DONE**: cross-family delphi (unanimous 20-issue set) -> frontier-core quorum -> AMENDED, no result retracted; 20 wording/citation edits committed `57df922`, render-confirmed, pushed. Stage 1b (citation-support) DEFERRED + redesigned as a repo-grounded citation audit (todo/039)
- [x] todo/039 citation audit DONE -- 69 attachments / 35 sources, repo-grounded (39-agent Workflow + independent verify of the flagged set); **66 supports / 2 partial / 1 misattributed, 35/35 identities OK**; 3 edits @`2ec106a`. Follow-ons: acquired **Asparouhov-Muthen (2010)** as the exact affine scaled-and-shifted anchor + cited @`3413eae` (Satterthwaite attribution corrected); **D-20** attribution 'AI-assisted' -> 'human-prompted AI derivation' @`5a109d3`. check_plan green, APA preview clean (85 paragraphs).
- [x] APA render promoted to CANONICAL + HTML pipeline retired (D-21, 2026-06-28) -- `build_manuscript_apa.py` is the sole builder (level3 -> `mi-spectral-apa.qmd` + tracked `.pdf`); both preview builds + the old `compile_manuscript.py`/`mi-spectral.qmd` deleted; all references repointed; S5 tables captioned + cross-referenced (#tbl-ladder/#tbl-structural-limits); S2-P5 sentence fix; D-20 id collision fixed. Commits `ba4d4be`+`aa4252a`, pushed.
- [x] Manuscript branch merged to main + PUBLIC RELEASE (2026-06-28..07-01) -- `contributions-delphi-todo035` merged (merge `21b75ef`, apaquarto + todo/036-039 + D-18..D-21); public repo live with a GitHub Pages site (`docs/`) + README; new title, human-prompted AI framing, ORCID, project-page link in the author note (`b0b2c0c`); transcripts link HELD pending a redacted `.claude` copy. HEAD `b0b2c0c` on `main`.
- [~] v1 AI-review response (paper 88) -- **DA-ENGINE THREAD CLOSED (2026-07-02, todo/040/040b/041; commits cc62ad7/f4900ec/948f8a9/2ed3f3a/0da1e52)**: cross-model derived (Fable 5) + independently audited (GPT-5.5 Pro blind+red-team, xhigh) + triaged (C9 scope condition revised, C6 mechanism corrected) + RATIFIED (Marcus, after a clean Consensus.app due-diligence pass) + LANDED in `derivation.qmd` (D9 :391-394; a new scope-and-correction callout + corrected fitted-vs-oracle callout :422-479; the known-scale addition :812-816; `decisions.md` D-22) + render-verified (`quarto render` exit 0, 20/20 Appendix B cells) + provenance verify-reads DONE for the four already-owned papers (Wang-Robins 1998, Nielsen 2003, Schafer 1997, Honaker 2011, all confirmed to support their attributed claims verbatim; litrev's RAG is down, file-RAG was the fallback). Theorem now scoped to second-moment-matched, *regular*-draw engines (FITTED, bootstrap/EMB = Amelia) with the IMB term also controlled; Bayesian posterior draws carry the disclosed gap D = -1/2 tr(RIV_mumu) - 1/2 sum tr(Vhat^-1 C b_Sigma C') - IMB. The remaining provenance piece is now DONE (2026-07-02): Marcus added von Hippel-Bartlett 2021 + Efron 1979 + the von Hippel small-sample paper (2016, resolved) to Zotero and re-exported BBT; all three ACQUIRED via `/lit-sync` (`literature/{vonhippelMaximumLikelihoodMultiple2021,efronBootstrapMethodsAnother1979,vonhippelNewConfidenceIntervals2016}.md`) after fixing a Drive-folder filename bug (the auto-namer dropped "hippel" from the two-word surname on both von Hippel PDFs; renamed to the BBT citekey for direct-match discovery). Not yet read-in-session/verified per todo/003's lifecycle -- do that before citing. Full staging/triage record: `todo/040-derivation-amendment-draft.md`; resume point if picked up cold: `todo/041-session-handoff.md`. Remaining for the OVERALL v1-review milestone: Phase 1 (fold DA evidence into the r1#8 response letter) + Phase 2 (BIC) + Phase 3 (non-normal, gated) + Phase 4 (Tier C/A fixes, letter, annotation + compile-enablement). Optional/cheap, not blocking: extend the E3 bootstrap-floor N-scan (gate item C7); verify Mode B's candidate IMB closed form.
- [ ] Per-paragraph annotation (Marcus passes) + compile-enablement (fold the 1a + todo/039 audit + AM2010 + D-20 edits + annotations; flip compile_enabled; the front matter is now ALREADY in the canonical APA build, so the remaining port is only into any second render target)
- [ ] Compile + derivation-audit (0 blocker/0 warn) + Zenodo deposit -> JAIGP submission

# Next 3 actions

1. **Read + verify the three newly-acquired provenance references, then move to Phase 1.** von Hippel-Bartlett (2021), Efron (1979), and the von Hippel small-sample companion (2016, SEM 23, DOI confirmed) are all ACQUIRED (`literature/{vonhippelMaximumLikelihoodMultiple2021,efronBootstrapMethodsAnother1979,vonhippelNewConfidenceIntervals2016}.md`, 2026-07-02) but not yet read-in-session/verified per todo/003's lifecycle -- do that before any `@cite` of them lands in `manuscript/`. This was the only item left open in the DA-engine thread; everything else in it (derivation, cross-model audit, triage, ratification, landing, render verification, the four already-owned papers' verify-reads) is closed.

2. **Phase 1 -- fold the DA evidence into the r1#8 response letter.** Confirmation-with-characterization: two independent Jeffreys engines agree to the 3rd decimal; Amelia's validation stands, its floor sits inside the (A)+(C) band; recentred-DA is the constructive remedy -- NOT the deflated-arm percentage story, which this session's triage found unreliable (the -0.244/-0.78 numbers stand; the "83%/13%/4%" explanation of them does not).

3. **Phase 2 (BIC) -> Phase 3 (non-normal, gated) -> Phase 4 (remaining fixes, letter, annotation + compile-enablement).** Phase 2: CHECK the phase2 cell RDS for raw per-rep `ell_com/barL/npar/trRIV` before deciding re-aggregate vs. a focused rerun. Phase 3 gated on the MVN-scope call. Phase 4: Tier C fixes, Tier A rebuttals, point-by-point letter, annotation + compile-enablement -> compile + derivation-audit 0/0 + Zenodo -> JAIGP. Optional/cheap along the way: extend the E3 bootstrap-floor exact-identity arm to the same 4 N-points Amelia's original scan has (closes gate item C7); verify Mode B's candidate IMB closed form (CAS or a numeric check against the measured -0.005/-0.0065). Frozen preregistered originals UNTOUCHED.

# Decisions made

- Scope = congenial MI + regular likelihood only (non-congenial / FIML / EM are separate universes).
- Three witnesses (W1/W2/W3) + two applications (LRT, IC); Le Cam power-dominance demoted to a Discussion conjecture.
- Reframed after Chan (2022, AoS) to a deviance-bias decomposition *upstream* of test calibration (lineage: Rubin -> Meng-Rubin -> Chan -> this work).
- Bias = +tr(RIV) - 1/2*tr(RIV); the opposite-sign terms cancel in the sum -- reported honestly.
- Custom Cholesky for the one-zero-constrained MVN MLE -- ~25x faster than lavaan; made M=200 W2 feasible.
- Six-section IMRaD, no Conclusion; Methods in factual passive voice; each gate paragraph states what it cannot catch. (SUPERSEDED 2026-06-27 by D-12: arc is now 7 moves -- a new "AI-human collaboration" move sits between Introduction and Background; still no Conclusion.)
- Human-prompted AI derivation with ORCID-verified provenance per JAIGP; no hallucinated citations (every citekey backed by a locally-read PDF).
- Level-3 paragraphs are the prose source of truth -- never edit the rendered qmd directly once compile is enabled.
- TRUST GATE (2026-06-16): hard evidence must survive adversarial/Socratic scrutiny before it enters the manuscript -- every figure tied to the exact claim it proves, no confident-but-wrong AI narration over results.
- S5 SOCRATIC REVIEW DONE (2026-06-26): cross-family tribunal (Claude + GPT-5.5 + Gemini via tkal-in-ket) -> repo-grounded defense -> press round. Verdict AMEND; no result retracted, no headline number changed. The two unanimous "blockers" were rebutted on the evidence (W1 pooled 2.43+/-0.26 CI [1.93,2.94] CONTAINS the registered 2.77; block-diagonal "overstates elsewhere" proved @prp-naive + measured Study 8). Method note: the tkal tribunal defender has no repo access and concedes everything; the informative pattern is challenges -> repo-grounded defense -> press round. Run: tkal-reviews/tribunal-20260627T014721Z/.
- W3-C = 1.000 UNDER UNCONGENIALITY is genuine, not an artifact: a preregistered directional prediction (todo/005 H2/H5) that held across all 60 cells. "100% land on saturated" confirms the bias DIRECTION but does not discriminate the decomposition (nested RIV-ordering makes the saturated model the natural overfit sink; rescoped in S5-P17). Uncongenial behavior disclosed in the new S6 G7 congeniality limit.
- TERMINOLOGY T-08 (2026-06-26): "oracle" (complete-data sense) -> "complete-data benchmark" in reader-facing prose. CLOSED 2026-06-27 (Marcus): conditioning-sense "oracle" -> "true-model expectation" (S3-P6/S4-P4/S4-P7); "oracle" no longer in level3 prose; figures regenerated with "complete-data benchmark".
- PROSE READABILITY (2026-06-26): the manuscript must run the full high-school paragraph arc (topic -> support -> wrap), state logical steps plainly (redundancy acceptable), read at a lower grade level.
- READABILITY PASS EXECUTED (2026-06-27): 67/82 level3 paragraphs rewritten; corpus FK 11.9 -> 10.1, mean sentence length 15.2 -> 11.1. No number/claim/hedge/citation/cross-ref/figure markup changed (mechanically verified). Method (reusable): calibrate on one paragraph against Marcus's eye, then scale under a strict spec + a mechanical token-preservation verifier. Commit 675323a, merged to main.
- G-NUMBER COLLISION RESOLVED (2026-06-27): congeniality limit -> G7 (after MVN G6); collective range G1-G7.
- todo/034 EXECUTED (2026-06-27): all five items on branch rendering-restructure-todo034; MERGED to main (FF to f666dc2, unpushed). Equation line-breaks, figure legibility (margin legends + alpha + halos + Okabe-Ito), arXiv template (55->24 pp), new S1B section.
- D-12 AI-COLLABORATION RESTRUCTURE (2026-06-27, Marcus, AskUserQuestion): SPLIT -- new early section "AI-human collaboration" (sec-ai-collab, S1B, M1b) between Introduction and Background; takes the novelty framing + relocated roles material; Methods drops to five parts. Inserted as S1B with NO renumber of S2 onward.
- arXiv PREVIEW FORMAT (2026-06-27): preview uses mikemahoney218/quarto-arxiv (arxiv-pdf, xelatex). manuscript/preview-jasa/ is gitignored; decisions.md is the tracked trace; canonical compile adopts arxiv-pdf when compile_enabled flips.
- FIGURE LEGIBILITY APPROACH (2026-06-27): base-R figures get bottom-margin legends (margin_legend helper), alpha ~0.72 fills, white-halo value labels, one Okabe-Ito palette.
- INFRA BUG (2026-06-27): the check_citations pre-write hook misfires on Edit/Write of files in manuscript/ SUBDIRECTORIES and on ANY Edit/Write while the shell cwd sits in a subdir (it resolves <cwd>/.claude/hooks/...). Workaround: write via Bash, or `cd` back to repo root before an Edit/Write. Easy fix: resolve the hook path against repo root.
- todo/035 PLACEMENT (D-15, 2026-06-27, Marcus via AskUserQuestion): Intro Contributions enumeration (S1-P6b) + a Discussion "Implications for applied research" passage (S6-P7b) -- TWO units. The abstract is given a tracked home (level3 top-level `abstract`, consumed by build_preview.py) and leads with the applied hook.
- DELPHI VETTING DONE (2026-06-27, tkal-in-ket pool-then-axes; opus-4-8 / gpt-5.5-pro / gemini-3.1-pro): pool 8/8 unanimous at round 5; axes 5 unanimous / 7 majority / 0 split, all KEEP-WITH-REVISION. Workflow ratified FIRST-CLASS (self-coding caveat in-sentence); applied-first lead = the tr(RIV_k) IC correction; three IC items consolidated to two; 8-item set sufficient. Artifacts: tkal-reviews/delphi-{pool,axes}-*/.
- D-16 AUTHORSHIP (2026-06-27, Marcus): corresponding author Marcus Waldman, Department of Biostatistics and Informatics, University of Colorado Anschutz Medical Campus, marcus.waldman@cuanschutz.edu (was single-author / Univ. of Nebraska Medical Center). AI co-authors in order with parent-company affiliations: Claude Opus 4.7-4.8 (Anthropic), GPT-5.5 Pro (OpenAI), Gemini 3.1 Pro (Google). Corresponding-author label in the title \thanks{} footnote. YAML in gitignored build_preview.py; decisions.md D-16 is the tracked trace.
- D-17 NO DIRECT QUOTES (2026-06-27, Marcus): no verbatim quotation of other work anywhere in the manuscript prose -- paraphrase/summarize + cite the stating passage. Amends T-06; CLAUDE.md updated. Applied to S1-P2, S2-P8, S2-P10. Scope = main manuscript (level3); derivation.qmd (sourced-audit companion, D-08) OUT of scope.
- tkal-in-ket GOTCHA (2026-06-27, reusable): the claude provider's `thinking_budget` param sends `thinking.type.enabled`, which opus-4-8 REJECTS (drop it). The engine `.env` supplies all three API keys even when the process env lacks ANTHROPIC_API_KEY; `tkal preflight` is the definitive check; axes batches all axes per call.
- VOCAB SOFTENING (2026-06-27): `scripts/corpus_vocab_check.py` = manuscript-vs-literature weighted log-odds (Monroe). Priority 1+2 applied (bf5b853): the engineering-metaphor cluster over-represented vs the 61 source papers and absent from them was plained (sits->lies/stand/fall, gate->check, tier->grade, pipeline->sequence, graded->assessed, pinned->traced, lineage->model family, sourced->checked-against-source). `scope`/`transcripts` kept (false positives). T-03 banned-vocab scan clean (0).
- ABSTRACT 1ST SENTENCE FIX (2026-06-27, Marcus): "biased toward the models that lost the most data" was nonsensical -- models do not lose data (all candidates fit the SAME imputed data); the driver is model-specific tr(RIV). Replaced (53b96bc) with "Model selection on multiply imputed data is biased toward more complex models, because their larger relative increase in variance makes the fit look better than it is" -- keeps the cause on RIV. (FURTHER REFINED 2026-06-28 by the todo/038 1a quorum: "more complex models" rescoped to "the candidates with more missing information, which in the nested families studied are the more complex models".)
- ACCURACY / OVERCLAIM PASS (2026-06-27): `scripts/overclaim_flag.py` scout (331 lexical candidates) + an adversarial workflow (per-section triage -> 3 diverse-lens verifiers proof/measurement/scope-skeptic, majority). 16 genuine suspects -> 15 confirmed 3/3, 1 cleared, 0 split; applied at 3d0d844. Each scoped to exactly what the proofs and measured results back: estimated-scale condition restored on the +1/2 tr(RIV) headline (S1-P4/S1-P6b/S6-P1; known-scale flips the sign), "exactly" -> "to leading order"/"we predict"/dropped on measured/leading-order quantities, "at the null" restored on the LRT differential (S1-P6), "restores" -> "substantially restores" on IC selection (0.17 weak-signal floor), and the workflow claims made defensible ("productive" -> "active and human-directed" (no solo baseline), self-coding caveat, "is public" -> "enters the public record" per D-13). Reusable: overclaim_flag.py + the workflow.
- PUNCTUATION BUDGET v2 (2026-06-27, Marcus): the v1 near-zero-connective rule forced every clause relationship into a period, leaving a monotone short-declarative column (and orphaned "it" pronouns). v2 (CLAUDE.md + check_plan gate, 99eca65): ENCOURAGE connectives between clauses (", because / , but / , so / , while / . However, / . Therefore,"); ALLOW semicolons but SPARSE (<=1 per paragraph, tightly-linked clauses, never a list); still BAN em-dash chains, nested glosses, list-semicolons; new "no orphaned pronouns" rule (name the noun). The check_plan prose gate now flags 2+ semicolons/paragraph and em-dashes, allows a single semicolon.
- CADENCE / STYLE PASS (2026-06-27): `scripts/cadence_flag.py` (monotony scout: length-uniformity, opener repetition, connective rate, orphan-pronoun openers) + per-paragraph rewrite + a 3-lens adversarial verify (fidelity/budget/cadence, accept only if all pass). 45 paragraphs rewritten (batch1 10 @ a1092bf, batch2 35 @ d6fc514); corpus connective rate 0.07 -> 0.16/sentence, sentence count 1101 -> 909. 36 paragraphs flagged by the strict cadence lens (not applied), S4-P6 skipped (token guard). GOTCHA: the rewrite agents emitted literal `\n` (backslash-n) for real newlines at heading/image boundaries in S4-P2/S5-P1/S5-P21, which broke the render (undefined control sequence) because the token guard checks math/numbers/citations NOT newline structure; fixed at af2f22c, and todo/036 must ADD a newline-structure guard to apply_cadence_batch.py.
- WORKFLOW PALATABILITY (2026-06-27, Marcus): the multi-agent Workflow tool is "problematic" for prose passes -- opaque auto-reject (36 paragraphs flagged invisibly), batch auto-apply, the `\n` corruption it hid, and an args-stringification bug that forced hardcoded id lists. For the tightening pass (todo/036) use per-paragraph human-in-the-loop with a SINGLE independent adversary via the Agent tool (NOT Workflow); the human is the gate, the adversary only surfaces objections.
- RENDER GOTCHA (2026-06-27, Marcus): a slow preview render is a one-time tinytex `tlmgr` package update over the network (log: "updating tlmgr"; a `curl` child is the tell). LET IT FINISH (background it so the 2-min tool timeout doesn't kill it); do NOT kill it mid-update or hand-roll a direct xelatex/.tex compile (Marcus called that churn "unhealthy"; killing it mid-update also makes the next render re-run it). Memory: feedback_render_use_standard_quarto.
- todo/036 TIGHTENING PASS EXECUTED + SHELVED (2026-06-27): Built the deterministic tooling -- a newline-structure guard on `scripts/apply_cadence_batch.py` (83e08ac; refactored the per-item check into a testable `check()`, rejects any rewrite that changes the real newline count or introduces a literal backslash-n outside math so `\nu`/`\neq` in math never false-trigger, closing the gap that broke the todo/035 render) and `scripts/fluff_flag.py` (7bed69e; filler/empty-phrase/force-metaphor density, worst-first, deliberately spares the accuracy-pass softeners + the contrastive "rather than"). FINDING: only 10 of 85 paragraphs carry ANY lexical fluff candidate, one hit each; the four prior passes already stripped the filler and the top hits are mostly load-bearing false positives ("this very work", "essentially complete" = a hedge). The dominant risk of a full sweep is RE-INTRODUCING overclaim by cutting a hedge. Marcus: "Not worth the risk" -- full 85-paragraph sweep SHELVED; curated-5 (S1-P3/S2-P13/S1B-P2/S5-P12/S6-P7) optional. The per-paragraph human-gated method (single Agent-tool adversary, NOT Workflow) is validated as the design even though it was not scaled.
- BIBLIOGRAPHY CONFIRMED (2026-06-27): references use the Better BibTeX export `literature/mi-spectral-better-bibTeX.bib` on ALL render paths (mi-spectral.qmd:8, derivation.qmd:6, build_preview.py:74 + the generated preview qmd); 60 entries, BBT firstauthorShortTitleYEAR convention (the old hand-maintained `mi-spectral.bib` was deleted during drafting). All 35 distinct citekeys used in level3 prose resolve (0 missing); 25 uncited superset entries are harmless. `.bib` last synced 2026-06-06 (b73d806) -- re-sync via `/lit-sync` if any new citation is added during annotation before compile.
- apaquarto APA PREVIEW BUILT (2026-06-28): `manuscript/preview-apa/` (gitignored, parallel to preview-jasa), built from level3 by `build_apa_preview.py`; renders the real manuscript in APA manuscript mode. Reusable gotchas (memory `project_apaquarto_preview`): TinyTeX 2025->2026.06 via `quarto install tinytex` (the 2026 repo rollover blocks cross-release package fetch -- distinct from the slow-render gotcha); `\let\openbox\relax` in header-includes for the apa7-vs-amsthm clash; the two-class author block via `\renewcommand` of apa7's `\displayauthors`/`\displayaffiliations` (apa7 parses names structurally, so those named macros are the override point, NOT the title.tex partial); `number-sections: true` + `number-depth: 1` so `@sec-` refs resolve (APA does not number by default).
- D-18 AUTHORSHIP AMENDS D-16 (2026-06-28, Marcus; logged decisions.md db2a0ac): JAIGP two-class author block -- AI Authors (Claude Opus 4.7-4.8, GPT-5.5 Pro, Gemini 3.1 Pro, listed first, NO institutional affiliation) and Prompters (Marcus Waldman, corresponding, full Center for Innovative Design and Analysis / Dept of Biostatistics and Informatics / Colorado School of Public Health / UC Anschutz affiliation + ORCID 0000-0002-3288-4803 shown in the author note). Parent-company names move from formal affiliations to the author-note prose; DRAFT CRediT roles (Marcus to finalize). Realized in the apaquarto preview; to be ported to arxiv + canonical at compile-enablement. Supersedes D-16's flat peer list.
- D-19 TITLE (2026-06-28, Marcus; db2a0ac): "Model selection after multiple imputation: a deviance correction for AIC, BIC, and likelihood-ratio tests" -- correction-led + applied-first (names the criteria), replacing the bias-led "The deviance bias of congenial multiple imputation, ...". Short title / running head: "Model selection after multiple imputation". "restores"/"valid" kept out of the title (substantially-restores, 0.17 floor).
- BRANCH-FLIP GOTCHA (2026-06-28, memory `feedback_verify_branch_before_render`): a concurrent session can `git switch` the working tree under you; manuscript builds read level3 from the ACTIVE branch, so a flip silently renders stale content. This session the tree flipped contributions-delphi-todo035 -> public-release-prep (older main+docs), and several renders surfaced a PHANTOM D-17 quote 'violation' + missing Contributions/abstract -- none real (the real branch's D-17 paraphrase bdc5a3a was intact). Tell: builder paragraph count 85->83. Confirm `git branch --show-current` before rendering.
- todo/037 QUEUED (2026-06-28, Marcus): a precision & accuracy restoration prose pass, the COMPLEMENT of the shelved todo/036 tightening (add precision, don't cut fluff). Root cause: the plainification arc protected tokens but not MEANING. Four reviewer-grade patterns (handoff bf2e77e): (1) name the direction not 'one/the other'; (2) join clauses to kill the orphaned 'it' + self-contained headers; (3) fix mechanism inaccuracies ('models don't lose data' -- per-candidate driver is tr(RIV); S1-P6b item 1 twins the abstract fix 53b96bc); (4) cut color commentary ('gives it its force'). Plus a standing accuracy invariant for CLAUDE.md + check_plan; same per-paragraph human-gated single-adversary mechanism.
- todo/037 EXECUTED (2026-06-28): all four patterns resolved per-paragraph human-gated (one Agent-tool adversary each; Marcus ruled every paragraph). S1-P6b mechanism ("lost the most data" -> "most missing information"); S2-P2 specificity (names $\hat\theta_{obs}$ read-as $\hat\theta_{com}$, direction proof-verified); calibration set S2-P2/S2-P9/S2-P7/S1-P4; batch 1 S2-P6/S3-P4/S3-P8/S6-P8/S3-P6 ("Load-bearing" cut). Color + mechanism scouts CLEAN corpus-wide; entry-point pronoun violations 0. Why: the readability arc protected tokens but not meaning; this restored the precision for an expert-reviewer audience. Commits 15e1b1b/2e9b28c/fb5e9eb/8d7eccf/eb6c155; check_plan green; APA preview clean (85 ¶).
- ACCURACY INVARIANT + COLOR GUARD (2026-06-28): two new CLAUDE.md prose-style standards -- "Accuracy invariant -- data quantity" (models never lose/gain data; per-candidate driver is the model's own tr(RIV), never an amount of data) and "No color commentary" (state the fact + consequence; show what changes, don't assert importance; "central" kept as the motivate-by-use opener) -- each backed by a `scripts/check_plan.py` guard (`prose_mechanism_hits` scoped to spare "missing data"/the S6-P7 analyst phrasing; `prose_color_hits` high-confidence, excludes "central") and a scout. Why: no mechanism-fidelity or color guard existed, so plainification could swap a precise driver for a wrong-but-fluent one and pass every check.
- TAIL-TRIAGE METHOD (2026-06-28, reusable): a low-yield pattern-2 tail (~20 orph=1 paragraphs) was cleared by ONE read-only agent classifying each KEEP/FIX against a strict bar (orphan only if antecedent 2+ clauses back / ambiguous / at an entry point) -> 0-fix/20-keep. Efficient way to clear a judgment-heavy tail without spawning 20 adversaries; the scouts are a lexical assist, the read is the catch.
- todo/038 DESIGN (2026-06-28, Marcus): the chunk was a rigorous repo-grounded defense of the manuscript TEXT AS WRITTEN via tkal-in-ket -- two parallel pool-mode delphis (1a claim-support from the paper's own evidence; 1b citation-support, panel challenges from training weights, defender rebuts with VERBATIM `literature/<citekey>.md`) -> one quorum where the in-session Claude defends the union per-issue (upheld/amend/refute). Locked: theory + sims OFF-LIMITS (S5-tribunal-defended); surface = abstract + §1/§1B/§2/§3/§5/§6, EXCLUDES §4; NO hard issue cap (severity judgment); 1b guard (specific recollection + confidence); defender is the repo-grounded in-session Claude, NOT the harness model. Why: the citation-discipline corpus makes the verbatim-vs-training-memory asymmetry win for the defense, directly stress-testing the #1 AI-paper failure mode. Handoff `ed0f33d`. (1b was later spun out -- see the todo/038 1a EXECUTED + 1b deferral decisions below.)
- tkal EFFORT FOR TEXT/CITATION REVIEW = NOT MAX (2026-06-28, Marcus): for a defense that is training-weight recall + within-text inference, the discriminator is cross-family DIVERSITY + careful READING + enough probing ROUNDS, not per-turn reasoning depth; and the 1b ground-truth check is the defender's verbatim quote (effort-independent). So: delphis on the frontier cross-family panel at normal effort; quorum at STANDARD tier with rounds preserved; do NOT drop to cheap open-weights (weak models misread dense methods prose + have poor recall of the specialized citations -> noisy 1b). Confirm the exact tier trade in the skill before locking.
- todo/038 STAGE 1a EXECUTED (2026-06-28): the claim-support text-validity defense is DONE. Cross-family delphi (pool mode, frontier trio at normal effort, Gemini routed via DeepInfra) converged UNANIMOUS on a 20-issue set (contested 34->10->0 over 4 rounds; no cap truncation, every issue incl=3) -> frontier-core quorum (in-session Claude defended repo-grounded; peers = GPT-5.5 lead direct + Gemini-via-DeepInfra; effort high, 3 rounds) -> determination AMENDED, no result retracted, all 4 rubric bars holds-as-amended, the 3 alleged-inaccuracy challenges (impossibility vs G4; nested-LRT conflation) defeated as presentation inconsistencies cured by the edits. 20 bounded wording/citation edits applied via `manuscript/plan/updates/update-textval-quorum-1a-20260628.py`, committed `57df922`, render-confirmed (APA preview clean, 85 ¶), pushed. Run record: `tkal-reviews/textvalidity-20260628T193253Z/` (delphi 1a-claim/REPORT.md; quorum DETERMINATION.md + EDIT-LIST.md). Representative edits: "restores complete-data-equivalent selection" -> "in expectation ... substantially restores"; impossibility claims aligned to G4; IC-ranking split from nested-LRT; "every/only" object-named; "bounds" -> "indicates"; counterfactual/history recast as interpretation; Satorra-Bentler cite added; "developed separately" -> future work.
- 1b CITATION-SUPPORT DEFERRED + METHOD CHANGED (2026-06-28, Marcus): the cross-family delphi for 1b was KILLED mid-run (the training-memory FIND step hung GPT-5.5 reasoning over ~27 citations from memory). Redesigned as a **repo-grounded direct citation audit** (todo/039, handoff `6732cbe`): read each `literature/<key>.md` (the verbatim source we own) and verify it against the attached claim; covers ALL sections incl. §4 (citation-appropriateness is section-agnostic, unlike the 1a overclaim review). Why: we hold the verbatim PDFs locally, so ground-truth-first beats a fallible-memory FIND step; faster and more reliable.
- tkal QUORUM TIER REALITY (2026-06-28): the quorum presets are DeepInfra-forward -- even "standard" = 2 cheap open-weights (GLM-4.7-Flash + DeepSeek-V3.2) + a frontier lead, SAME round cap (3) as "max"; the tier knob trades PEER COMPOSITION, not rounds. So todo/038's "standard tier" instruction self-contradicted its "no cheap open-weights" instruction. Resolved with a CUSTOM frontier-core config (override the preset): peers = openai (GPT-5.5, lead, direct) + gemini, effort high (not xhigh), 3 rounds, NO open-weights. Gemini routed via DeepInfra (`routes`+`models` google/gemini-3.1-pro) to avoid mid-run rate-limit (Marcus). This SUPERSEDES the "confirm the tier trade" open item.
- tkal QUORUM "REFUTED" AGGREGATION BUG (2026-06-28): `collect_sustained_objections` computes `is_inacc = so.is_inaccuracy OR by_id[oid].is_inaccuracy`, OR-ing the PROBE-TIME inaccuracy flag; so a determination that merely re-lists an originally-inaccuracy-tagged objection as an edit item trips the `sustained_inaccuracy` gate and forces class "refuted" -- even when BOTH peers returned `determination:"amended"` with `is_inaccuracy:false` and all bars holds-as-amended. Read the per-peer `determination.json` (its `determination` + per-objection `is_inaccuracy`), NOT the printed headline class. One-line fix worth making in `~/git-repositories/tkal-in-ket` (use the determination-stage flag, or skip `withdrawn` objections).
- DUE-DILIGENCE-AGAINST-THE-DATA (2026-06-28, Marcus): verify each claim's wording against the source/results, not the prose or a panel's assumption. In 1a the quorum suggested adding a "congenial" qualifier to the W3 100% claim; the actual results (`verification/cache/run_all-regen/phase2-w3/summary.csv`, `W3C_high_RIV_frac=1.000` in all 60 cells; verdicts H2/H5) showed it held everywhere, so the qualifier was DROPPED and the abstract/intro state it plainly ("in these simulations, 100%"). Also (Marcus, anti-bragging): "entirely" dropped; "100%" stated as a plain empirical fact, not a boast.
- todo/039 CITATION AUDIT EXECUTED (2026-06-28): 69 `@citekey` attachments across 35 sources, repo-grounded against the owned `literature/<key>.md` via a 39-agent Workflow (one judge/source + an independent cold-read verify of the flagged set = the self-rubber-stamp guard, which flipped the Meng verdict partial->misattributed). Result 66 supports / 2 partial / 1 misattributed; 35/35 identities resolve (incl. the Zotero-style keys Meng01121991, Tierney01031986). 3 edits @`2ec106a` (S2-P7 drop @meng -> Nielsen 2003 carries 'congeniality implies properness', not Meng's text; S5-P6 reframe the SB cites as the tradition; S4-P22 'developed separately' -> future work). Vector-RAG (litrev/MotherDuck) was DOWN all session -- file-RAG over the owned corpus is todo/039's mandated arbiter anyway. Artifacts: todo/039-citation-audit-{results.md,checklist.md,claim-map.json}. Reusable: due-diligence FLIPPED the audit's S5-P6 recommendation -- the W2 C5 code computes a genuine affine a*T+b, so the prose was right and the CITATION was the defect, not the wording.
- AM2010 ANCHOR ACQUIRED + SATTERTHWAITE CORRECTED (2026-06-28): the S5-P6 affine scaled-and-shifted statistic's exact source is **Asparouhov & Muthen (2010) 'Simple Second Order Chi-Square Correction'** (eq.4 T3=aT+b, fixed df; nested-difference version in sec.4 = the W2 LRT setting); acquired Zotero->Drive->Mathpix->lit-sync, read in-session, cited @`3413eae`. The Satterthwaite (1946) attribution was corrected: it is the T2/effective-df origin (= the already-owned @asparouhov2006), NOT the affine source (todo/003 line-41 superseded). Reusable gotchas: (a) `zotero_add_paper` with authors as ONE string -> BBT pins a malformed citekey (`tihomirasparouhov...`); user re-pins in Zotero (Extra: Citation Key) + re-export; (b) the acquisition->cite chain is MotherDuck-INDEPENDENT (convert_pdf + lit_sync need only Mathpix + the SHA256 cache; only index_papers needs MotherDuck) -- a citation can be added during a RAG outage.
- D-21 APA RENDER IS CANONICAL + HTML PIPELINE RETIRED (2026-06-28, Marcus, AskUserQuestion "promote APA build to canonical"; decisions.md row D-21): the apaquarto-pdf is the official manuscript render, no longer a "preview". `scripts/build_manuscript_apa.py` (promoted from the gitignored `build_apa_preview.py`) builds `manuscript/mi-spectral-apa.qmd` from level3; the apaquarto extension is vendored at `manuscript/_extensions`; `mi-spectral-apa.qmd` + the rendered `.pdf` are tracked (LaTeX intermediates gitignored). Both preview builds (`preview-jasa/`, `preview-apa/`) DELETED -- the JASA/arXiv build is retired. The old HTML pipeline (`scripts/compile_manuscript.py` -> `mi-spectral.qmd`) is fully retired: script + frozen qmd + its render deleted, all live references (CLAUDE.md, plan/README.md, level3 purpose/compile_note, status.json, the section-review/status/derivation-audit skills, the citation-hook test) repointed; `compile_enabled` kept as the prose-lock gate, decoupled from the deleted compiler. The D-18 author block + D-19 title now live in the canonical APA front matter (no longer preview-only). NOTE the apaquarto `apaoneauthoraffiliation.lua` ERROR lines for the affiliation-less AI authors are NON-FATAL (render exits 0; see open questions).
- D-20 ID COLLISION FIXED (2026-06-28): the apaquarto-canonical decision initially reused id D-20, which last session's attribution-terminology decision already owns; renumbered the apaquarto decision to D-21 (commit `aa4252a`). D-20 = attribution terminology; D-21 = APA canonical.
- S2-P5 SENTENCE FIX + REVIEW-LANDING VERIFIED (2026-06-28): Marcus flagged that a previously-suggested edit to "The E-step computes Q(.|phi) and the M-step maximizes it. Their heuristic is direct." never landed, worrying the Delphi/quorum edits had not reached the level3 source of truth. VERIFIED the opposite: the todo/038 (`57df922`), todo/039 (`2ec106a`), todo/037 precision commits all DO modify `level3-paragraphs.json`. This one sentence was byte-identical from the original S2 draft; no update script or review artifact touched it -- the suggestion was LOST (never captured into the pipeline), not rejected by any review (text-validity was claim/citation-scoped, todo/037 four-pattern-scoped). Fixed to "The reasoning is direct." Reusable: a section-review #[COMMENTS] suggestion only lands if it produces an update script under `manuscript/plan/updates/`; otherwise it leaks.
- TABLE CAPTIONS (2026-06-28): the two S5 tables had no captions/ids; added Quarto captions + ids (`#tbl-ladder` true-model selection rates; `#tbl-structural-limits` structural checks) and swapped the in-text "reported below"/"the table" phrases for `@tbl-` cross-references, so they number as Table 1 / Table 2. Fixed at the level3 source, not the rendered qmd.
- D-20 ATTRIBUTION TERMINOLOGY (2026-06-28, Marcus, AskUserQuestion; logged decisions.md @`5a109d3`): 'AI-assisted derivation' -> 'human-prompted AI derivation' across 9 binding+guidance files (21 occurrences). Why: under D-18 the AI are the AUTHORS and the human is the PROMPTER, so 'assisted' inverts the roles (frames a human author with AI help); 'human-prompted AI derivation' states the direction right (the AI derived, the human prompted). Chosen over 'AI-prompted' (parses as prompted-BY-an-AI) and 'AI-generated' (drops the prompting emphasis). Where 'human prompter' sits adjacent the redundant 'human' is dropped. Frozen tkal-reviews/ + historical scripts/handoffs retain the old phrasing (record not falsified).
- v1 AI-REVIEW RESPONSE PLAN (2026-07-01, Marcus): respond to the paper-88 review (`manuscript/reviews/v1/ai-review-paper-88.md`) with EVIDENCE for the two cheap gaps, not only rebuttals. Triage = 4 tiers (A already-in-paper/rebut, B the BIC decision, C easy fixes, D modest new content). Order of operations: Phase 0 smoke -> 1 DA replication -> 2 BIC -> 3 non-normal (gated) -> 4 prose + response letter. "Leave space" (Marcus): the forks are gates, not pre-solved. First artifact = a `todo/040` registration doc BEFORE any engine code (prereg discipline).
- DA ENGINE STRATEGY (2026-07-01): two engines. (i) hand-rolled CONJUGATE DA `impute_mvn_conjugate()` -- P-step Sigma ~ Inv-Wishart(n-1, (n-1)S), mu|Sigma ~ N(ybar, Sigma/n); I-step missing|observed ~ conditional normal (= posterior-predictive draw), ALTERNATED (our non-monotone pattern has NO closed-form one-shot; the iteration IS data augmentation). Cheap, exact-conjugate, auditable, uses R's own RNG. (ii) brms/Stan as a general-family imputer -- posterior-predictive completions fed into our EXISTING deviance/tr(RIV) analysis (brms-AS-IMPUTER, NOT the one-stage `bf(y|mi())` full-Bayes, a different estimand; NOT `brm_multiple` pooling). brms unifies DA (r1#8) + non-normal (r3#2). Do NOT depend on `norm::da.norm` (parallel-seed + single-precision fragility; the `rngseed` gotcha is handled only in the serial `00-test-primitives`).
- BIC APPROACH (2026-07-01): the +tr(RIV) deviance correction transfers to BIC VERBATIM (both take -2 barQ as the deviance numerator; the correction lands on the numerator, not the penalty). Sidestep the effective-sample-size controversy by using the COMPLETE-DATA n in the penalty -- the target is complete-data-equivalent selection, so compare against the complete-data BIC's own n rather than inventing an MI-specific effective n. Reviewer r3#1's effective-n objection then becomes a scoping sentence. Story to show: uncorrected MI-BIC (heavier complexity penalty) may partly resist the saturated pull, but still tilts toward higher-RIV among the models it weighs; only the corrected version tracks the complete-data BIC ranking.
- NON-NORMAL SCOPE (2026-07-01): MVN-only stays the DEFAULT (project_scope_keep_mvn); a non-normal empirical cell is a real extension that TRIPS the scope decision and needs Marcus's explicit call + a decisions.md entry. The theory PREDICTS the split (estimated-scale families keep +1/2 tr(RIV); scale-free logistic/Poisson collapse to the known-scale case, 0 under FIML / -1/2 tr under proper MI), so the honest free answer is a Discussion prediction paragraph; brms only makes an EMPIRICAL illustration tractable, and even then the analytic tr(RIV) for a GLM is OPTIONAL (sample tr(RIV) suffices for an illustration).
- PHASE 0 EXECUTED + DA-ENGINE FINDING (2026-07-01): the DA-replication gap (r1#8) is a REAL result, not a quick rerun. Exact Bayesian posterior-draw MI carries a *persistent* O(tr(RIV)) deviance discount vs the FITTED/+1/2 headline: Jeffreys IW (my conjugate == Schafer `norm::da.norm`, identical to the 3rd decimal, -0.783 at N=200, a+b/N asymptote ~ -0.74), Gaussian/Laplace draw -0.11 (a persistent JENSEN term -- imputing with a *drawn* Sigma inflates E[Sigma_{M|O}] because the Schur complement is nonlinear; O(1/N)/cell x O(N) cells = O(1)), brms weakly-informative -1.36. Confirmed to R=20000 (`verification/spikes/phase0a-confirm-xl.R`). Decomposition of -0.74: removable Sigma-mean inflation (N-1)/(N-p-2) (~69%, -0.54) + IW skew (-0.12) + Jensen (-0.11). Engine mechanically validated (NOT a coding bug): I-step exact (fixed-theta reproduces FITTED to -0.04+/-0.17), theta* dispersion matches analytic I_obs^-1, 0 PSD events over 100 reps x 20 workers, RNG bit-reproducible. This is exactly reviewer r1#8's concern and touches the FITTED-vs-ORACLE seam (project_fitted_vs_oracle_sign). Registration `todo/040`; memory `project_da_engine_finding`.
- DISCRIMINATING TEST -- AMELIA CONVERGES, BAYESIAN DRAWS PERSIST (2026-07-01): paired FITTED-relative N-scan (`verification/spikes/phase0a-calibrated.R`). **Amelia's bootstrap EMB CONVERGES to +1/2** (-0.099/-0.047/-0.029/-0.019 at N=200/500/1000/2000, ~O(1/N)) -- the paper VALIDATED with Amelia, so its empirical claim is asymptotically sound (its N=200 shortfall = the vanishing finite-N term). But the correct MLE-centered **Gaussian/Laplace draw PERSISTS at -0.106** (no prior inflation -- pure Jensen). So the -0.74 (IW) = removable prior mean-inflation (-0.54) + IW skew (-0.12) + persistent Jensen (-0.11). Both prior speculations partly right; NEW open puzzle: why does the bootstrap uniquely escape the Jensen term while every genuine Bayesian posterior draw (Gaussian/IW/brms) carries it? APPLIED consequence UNHARMED: the term scales with each candidate's own tr(RIV), so it REINFORCES the paper's selection direction (high-RIV favored), changing the correction magnitude, not the sign. So the paper's Amelia validation stands; a reviewer using a standard Bayesian DA (norm/brms/mice) would see the persistent branch.
- brms GATE + norm CROSS-CHECK RESOLVED (2026-07-01): brms `bf(X1|mi())+...+set_rescor(TRUE)` (cmdstanr/cmdstan 2.37.0 backend) fits and returns posterior-predictive completions; **runtime ~24s/fit (108s cold compile) -> CROSS-CHECK / NON-NORMAL-ONLY, NOT a workhorse** (a full R=1000 sweep would be hundreds of hours; focused cells fine). `norm::da.norm` (the prior-flagged fragile engine) IS usable for a SMOKE cross-check when parallelized via PER-REP `rngseed(base+r)` (each rep fully determines its own norm RNG -> no cross-worker collision; validated -0.741 parallel R=8 vs -0.743 serial anchor, -0.783 at R=10000 == conjugate). It reproduces the conjugate result, confirming the finding is a property of the METHOD, not my implementation.
- CROSS-MODEL HANDOFF FOR FABLE + COMMIT (2026-07-01): drafted `todo/040-cross-model-derivation-handoff.md` (self-contained: setup, how the derivation treats proper MI @sec-qfun, the powered evidence table, the amelia-vs-Bayesian-draw divergence, Q1-Q4) + `todo/040-cross-model-prompt.md` (cover prompt). Marcus sends through Fable (overriding the GPT-5.5 default of feedback_crossmodel_derivation_protocol this once -- "now that Fable is back"). Key grounding: the paper's proper-MI verification (`rederivation-L0-fitted-vs-oracle.R`) only ran the DETERMINISTIC FIML plug-in + Amelia (never an exact posterior-draw DA), and its known-scale proper engine (`rederivation-knownvar.R`) draws mu~N(theta_obs,I_obs^-1) with Sigma FIXED (so no draw-Sigma Jensen). Committed Phase-0 (registration + engine + spikes + handoff, 15 files) as 91da4df on branch `da-engine-phase0-todo040` (off main @ 07f5cfd, UNPUSHED); frozen originals untouched; reproducible spike caches left untracked per repo pattern.
- FABLE AUDIT VERDICT (2026-07-01/02, in-repo, max effort; `todo/040-audit-response-fable5.md`, commit cc62ad7): **option (i), theorem intact when scoped.** Exact per-unit identity (no Taylor step, evaluable from parameter draws alone): the engine-vs-FITTED gap is D = -1/2 sum_i { E[d'Vhat^-1 d] + tr(Vhat^-1(E V(Sigma*) - Vhat)) }, which reduces at persistent order to the master formula D = -1/2 sum_i tr(Vhat_i^-1 C_i (b_Sigma + Var(mu*)) C_i') - IMB. The +1/2 tr(RIV) theorem holds for engines with Xi = b_Sigma + Var(mu*) = o(1/n)/cell ("second-moment-matched": FITTED trivially; bootstrap/EMB via the exact identity E*[Sigma~ + (mu~-muhat)^{x2}] = Sigmahat, so Amelia's validation stands); Bayesian posterior draws have Xi > 0 by construction and carry -1/2 tr(RIV_mumu) - 1/2 sum tr(Vhat^-1 C b_Sigma C') (prior-dependent through b_Sigma only). MECHANISM CORRECTIONS: the handoff's "Schur-Jensen inflation" is refuted (Schur complement is matrix-CONCAVE; the curvature term cancels the Sigma-mean-wander exactly under MCAR) -- the persistent Laplace piece is the mu-wander -1/2 tr(RIV_mumu) = -0.1011 (mu-only arm matches to 4 decimals; = the known-scale C_n's mu-block, so the estimated-scale "proper = FIML alike" text contradicted the paper's own boundary case); the "IW-shape -0.12" is dissolved (anisotropic residual of the SCALAR deflation; posterior skewness is O(1/n) total; matrix recentring collapses IW -> Laplace); the Jeffreys complete-data inflation constant is (p+2)/n relative to the MLE, not (p+1)/n (Schafer 1997 :3502-3532), pattern-anisotropic under missingness (diag-avg kappaN 7.07, contraction-weighted ~11.3 vs 6). NEW: a small MAR-only design-imbalance term IMB ~ -0.006 tr(RIV) (exactly 0 under MCAR, both measured), carried by every drawing engine incl. Amelia -- the drawing-engine cousin of (A)+(C). All three branches reproduced by the identity: IW -0.787 (engine -0.783), Laplace -0.124 (-0.121), boot+EM -0.088 (Amelia -0.087); Laplace asymptote -0.1011-0.006 = -0.107 (handoff's -0.106 confirmed, composition identified). Evaluator `verification/spikes/audit-engine-formula.R` (arms: full/mu-only/Sigma-only, matrix-recentred conjugate, structural bootstrap+EM, MCAR discriminator, imbalance/matched split). GOTCHA: Amelia's `a$mu`/`a$covMatrices` are on an internal reordered/standardized scale (boot.type='none' does NOT reproduce the EM MLE) -- never harvest them; use the structural emulation.
- PROVENANCE ACCOUNTING FOR THE SCOPE AMENDMENT (2026-07-02): corpus ALREADY holds Wang-Robins 1998 (type-A/B), Nielsen 2003 (properness = variance-rule property), Schafer 1997 (inverted-Wishart mean incl. the m>=p+2 condition), Tanner-Wong 1987, Tierney-Kadane 1986, van der Vaart 1998, Honaker 2011 (the todo/003 "pending PDF" row was STALE -- full ~100KB conversion; corrected). Genuinely missing acquisitions: von Hippel-Bartlett 2021 (PDMI/MLMI naming, HIGH) + Efron 1979 (bootstrap bias estimation, HIGH if the bootstrap-mechanism sentence enters the manuscript; note `efronAssessingAccuracyMaximum1978` is Efron-HINKLEY, a different paper) + Robins-Wang 2000 (optional) + the Burkner/Stan/LKJ trio (CONDITIONAL on the brms cell entering manuscript/ rather than the response letter). The 3-line inline-provable steps (exact identity, envelope, Schur concavity, the complete-data bootstrap identity) need NO external citation under the repo's prove-inline norms. The bigger provenance lift is IN-REPO: GPT-5.5 cross-model gate on Fable's new derivations, CAS files for the four lemmas, promotion of the spike to a verification module with pinned cache + [mc] hooks, level3/check_plan passage + decisions.md entry.

- ENGINE-GAP VERIFICATION APPARATUS BUILT (2026-07-02): `verification/cas-wolfram/verify_engine_gap_sympy.py` checks Fable's four algebraic lemmas (V'=CDC', V''=-2 Bdot Soo Bdot', the cancellation, C'V^-1C=[I_mis]_mumu) symbolically in a concrete p=3 (1 missing/2 observed) instantiation, plus a p=1 closed-form engine-gap toy table derived independently by hand THEN confirmed symbolically (univariate, entirely-missing-unit MCAR toy: tr(RIV_mumu)/tr(RIV)=1/2 exactly, giving Laplace -1/4 exact for all n, Jeffreys -n/(n-3) -> -1, bootstrap 0 exact) -- this toy table turned out to match the gate package's own withheld grading key exactly, an independent cross-check before the key was ever read. `verification/_modules/engine-gap-audit.R` promotes `spikes/audit-engine-formula.R` to the repo's cached-RDS + summary.csv module convention (pinned seeds unchanged; NOT wired into run_all.R's phase dispatch, left for a follow-up once the framing is ratified). Both artifacts pass/reproduce cleanly; neither is cited in `manuscript/` yet.
- run_layer3_openai.py EXTENDED FOR -pro MODELS (2026-07-02): `gpt-5.5-pro` is not served by the Chat Completions endpoint (404 "not a chat model") -- added a Responses-API path (`call_responses()`), submitted with `background=true` and polled (GET /v1/responses/{id}) rather than held on one blocking multi-hour socket read, so a transient network failure during a many-hour xhigh/pro run doesn't lose all progress. Confirmed live against the API (not assumed) that the valid `reasoning.effort` set is `none|minimal|low|medium|high|xhigh` -- there is no "max" tier on OpenAI's API; that vocabulary is the Claude Code harness's own effort scale, not transferable. Both findings gate-tested before the real (paid, multi-hour) run, not discovered mid-run.
- GPT-5.5 PRO GATE RUN + TRIAGED (2026-07-02, f4900ec): sent `todo/040b-gpt55-gate-package.md` end to end (Mode A blind ~45min, Mode B red-team ~70min, both xhigh). Mode A independently reproduced the core mechanism without seeing Fable's answer (Schur concavity -- deflation not inflation; E2/Jeffreys ~7x E1/Laplace via a Sigma-centering constant of 0/(p+2)/-1 for Laplace/Jeffreys/bootstrap; E3/bootstrap -> 0 exactly). Mode B's claim-by-claim verdict: SUSTAIN C1/C2/C4/C5; ESCALATE C3 (wording -- "exact under MCAR" needs an asymptotic not finite-sample qualifier), C6, C7; REFUTE C9 AS LITERALLY STATED; SUSTAIN-with-a-genuine-closed-form C8. Per the package's own escalation policy ("any refutation with algebra goes back through the evaluator"), triaged C9 and C6 before accepting anything (see the two entries immediately below); C7 flagged as a new open question, not yet triaged (no numerical claim to check, just an evidence-coverage gap).
- C9 SCOPE CONDITION REVISED (2026-07-02): Mode B's three counterexamples against the drafted "$\Xi=o(1/n)$ per cell, necessary and sufficient" scope statement all held up under scrutiny -- (1) an engine with $\Xi=0$ can still have $D\neq0$ under MAR, because the design-imbalance (IMB) term is a SEPARATE additive piece in Fable's own C4 master formula, never absorbed into $\Xi$; (2) an adversarially-constructed bias direction lying in the kernel of the missing-information contraction functional can have $\Xi\neq o(1/n)$ with $D=o(1)$ anyway (a measure-zero technicality for any engine actually used, not a practical concern); (3) a rare-large-jump engine with zero mean bias shows the regularity/locality of the draw law (Op(n^-1/2), controlled third moments) was an unstated hypothesis behind Fable's own order-bookkeeping argument. `todo/040-derivation-amendment-draft.md`'s Amendment 2 callout is revised: states $\Xi=o(1/n)$ AND the imbalance term controlled, for a regular draw law, as SUFFICIENT (not an unqualified iff), and names the regularity condition explicitly. No number in the paper changes; only which engines the scope sentence covers.
- C6 "-0.244 DEFLATED ARM" -- NUMBER CONFIRMED, MECHANISM CORRECTED (2026-07-02): Mode B recomputed what a pure scalar deflation c=(N-p-2)/(N-1) of the conjugate engine's Sigma* draws should give algebraically (~-0.34 to -0.39) and flagged a ~30% gap against the round-1 reported -0.244 (I independently re-derived the same ~-0.344 by hand before checking empirically, so this was not a Mode B arithmetic slip). Checked with the exact-identity evaluator (R=60, paired same-seed draws, reproducing `diag-nscaling.R`'s exact deflated P-step): **the -0.244 number is solid** (reproduced at -0.2399, se .0075, well within 1 SE) **but the linear-rescaling mechanism both Mode B and Fable's own writeup assumed is wrong.** `diag-nscaling.R` deflates the scale matrix INSIDE the P-step of an iterative data-augmentation chain, not as a one-shot rescale of an otherwise-unmodified chain's stationary output (unlike matrix recentring elsewhere, e.g. `enga_recentre_sigma`, which IS a clean post-hoc shift and unaffected by this) -- the deflated Sigma feeds back through the I-step into a less-dispersed imputation, hence a smaller SS at the next P-step, and the chain settles at a materially SMALLER stationary bias than linear scaling predicts (measured b_Sigma contraction -0.1010 vs -0.343 predicted, a 3x miss). Consequence: Fable's "83% centering bias / 13% mu-wander / ~4% imbalance-plus-transient" percentage decomposition (audit response sec.2, "Correction to the handoff's expected decomposition") rests on this same flawed isotropic-vs-anisotropic reading of -0.244 and should NOT be carried into the manuscript or the response letter. This does not touch anything already staged: the Amendment 2 callout cites only the matrix-recentred value (-0.150) and the full Jeffreys value (-0.78), never the scalar-deflated-arm number or its percentage story. Reproduction: `triage-c6-deflated-arm.R` (this session's scratchpad; not yet promoted to the repo).
- MARCUS RATIFIED THE DA-ENGINE SCOPE AMENDMENT (2026-07-02, decisions.md D-22): after three Consensus.app due-diligence queries surfaced no contradicting or preempting literature (one new candidate reference logged, `todo/003`, commit `0da1e52`), Marcus ratified `todo/040-derivation-amendment-draft.md` as staged. Landed verbatim: the D9 forward-pointer, the corrected fitted-vs-oracle callout + new scope-and-correction callout, the known-scale addition; `decisions.md` D-22 records the ratification plus the superseding note on the retracted "83%/13%/4%" percentage story. Commit `2ed3f3a`; render-verified (`quarto render` exit 0, 20/20 Appendix B cells execute).
- CITATION-DISCIPLINE + RAG CHECK CLOSED FOR THIS LANDING (2026-07-02): Marcus asked directly whether full RAG/provenance diligence was done. Checked, not assumed: the landed text has zero external `@citekey`s (confirmed by grepping the actual commit diff), so the citation hook had nothing to enforce here. litrev's vector-RAG (MotherDuck) is DOWN (live-checked, `STATUS_ERROR` / "Could not connect to MotherDuck" -- same outage as todo/039's citation audit). Fell back to the established file-RAG-over-owned-corpus method: re-read the four already-acquired papers Fable's audit leaned on and confirmed each supports its attributed claim verbatim -- Wang-Robins 1998 (type-A/B distinction), Nielsen 2003 (properness = Rubin's-rule-unbiasedness), Schafer 1997 `:3502-3532` (IW mean, m>=p+2), Honaker 2011 (EMB algorithm). Only the NOT-yet-acquired references (von Hippel-Bartlett 2021, Efron 1979, the new von Hippel small-sample paper) remain open, and that requires Marcus's Zotero action, not more RAG.
- DRIVE-FOLDER PDF NAMING GOTCHA (2026-07-02): the litrev migration/auto-naming step drops the second word of a multi-word author surname when generating a Drive-folder PDF filename -- "von Hippel" became `von_..._<year>.pdf` with "hippel" missing entirely on both von Hippel PDFs, which broke `lit_sync.py`'s surname-substring PDF discovery (`SKIP: PDF not found`) even though the files were physically present and already Mathpix-converted. Fix: rename the Drive PDF to `<citekey>.pdf` exactly, which hits `lit_sync.py`'s direct-match Pass 0 (the same working fallback already used for `Meng01121991.pdf`/`Tanner01061987.pdf`/`Tierney01031986.pdf` in that folder); the rename doesn't touch the SHA256-keyed Mathpix cache lookup, so no reconversion is needed. Check the actual Drive-folder filename before assuming a "PDF not found" skip means the PDF is genuinely missing. Reusable memory: `feedback_acquisition_pipeline`.

# Open questions

- **IMB closed form (2026-07-02):** Mode B (GPT-5.5 Pro red-team) delivered a CANDIDATE closed form under this exact probit-MAR design (Tallis truncated-bivariate-normal moments of $Y_O\mid$ pattern; `verification/cache/layer3-gpt-5.5-pro-da-engine-modeB.md` sec. 4). Not yet independently verified (CAS or a numeric check against the measured -0.005/-0.0065) -- do that before citing it, or keep the manuscript sentence scoped to measured-and-bounded if it doesn't check out.
- **C7 bootstrap-floor evidence gap (2026-07-02, new):** the exact-identity "E3 master" 2-point comparison (N=200/1000) doesn't clearly show 1/n decay on its own (GPT-5.5 Pro Mode B flagged this). The paper's actual validated claim rests on the original 4-point Amelia N-scan (`phase0a-calibrated.R`, clean O(1/N) decay to -0.019 at N=2000), which is unaffected -- but the newer exact-identity mechanism decomposition doesn't yet have the same N-coverage. Cheap fix: extend `engine-gap-audit.R`'s E3 arm to N=500/2000 (parameter-draw-only, no imputation). Not blocking.
- **brms cell: manuscript or response-letter-only (2026-07-02):** if the -1.36 weakly-informative number is printed in `manuscript/`, the Burkner/Stan/LKJ citation trio (todo/003, conditional) activates and a higher-R brms cell is needed (current se .15 at R=6); if letter-only, mark the trio rejected. Interacts with the Phase-3 MVN-scope gate.
- BIC: re-aggregate from the phase2 cache vs a focused rerun -- depends whether the cell RDS stored raw per-repetition `ell_com/barL/npar/trRIV` or only selection labels (CHECK first, before Phase 2).
- Congeniality of the brms imputer to each non-normal analysis family (trivial for unstructured MVN; needs thought per GLM family), and which non-normal families best exhibit the two-regime split (Gamma / t for estimated-scale; Bernoulli / Poisson for the collapse). Phase 3, gated on the MVN-scope decision.
- r3#3 congeniality-diagnosis needs a CITED method -- log candidate references to `todo/003` before drafting that Discussion sentence.
- Sequencing: does the review-response cycle run BEFORE, or interleaved WITH, the per-paragraph annotation + compile-enablement (both feed the same revision toward submission).
- Optional residual prose work, deferred (do at/before compile-enablement or skip): the todo/037 optional S4-P1 "It prices the bias" -> "The theorem prices" (triage KEEP, zero-risk); the curated-5 tightening mini-pass (S1-P3, S2-P13, S1B-P2, S5-P12, S6-P7) and/or a softer cadence "pass-3" on the 36 strict-cadence-lens-flagged paragraphs (ids in todo/036-tightening-pass-handoff.md). The full todo/036 tightening sweep was shelved as not-worth-the-risk; this is the residual, and the tooling is ready.
- apaquarto AUTHOR-AFFILIATION render noise (now in the CANONICAL build): the apaquarto `apaoneauthoraffiliation.lua` prints ERROR lines for the affiliation-less AI authors (D-18), but they are NON-FATAL -- the render continues via the `\displayauthors` override and emits the PDF (exit 0). Decide whether to silence them (a hidden placeholder city on the AI authors, or an apaquarto suppress option) for a clean log. Minor.
- Front-matter port (LARGELY RESOLVED by D-21): the D-18 two-class author block + D-19 title + ORCID + full affiliation now live in the TRACKED canonical APA build. STILL OPEN only if the submission adds a SECOND render target (e.g. arxiv/standard-Quarto): the apa7 `\displayauthors` override is apa7-specific, so the two-class block would need its own mechanism there. decisions.md (D-11/D-16/D-18/D-19/D-21) is the tracked trace.
- Discretionary items for Marcus's annotation pass: (a) trim S1-P6's inline "five new things" enumeration if redundant with the new S1-P6b Contributions list; (b) applied-first contributions ordering (IC correction leads, theorem second) -- keep or flip; (c) the workflow first-class framing carries GPT-5.5's minority "supporting unless caveat prominent" as the in-sentence self-coding caveat -- confirm; (d) the 5 wording deviations the 1a edit applied vs the verbatim quorum EDIT-LIST (TV-09/12/10/14/15) -- review in context.
- apaquarto front-matter discretionary (Marcus, at annotation): finalize the DRAFT CRediT roles; add the full postal address to the correspondence line (currently email only); keep or drop the parent-company names in the author-note prose.
- Real-data demonstration (HRS wealth components vs NHANES lipids) -- defer unless the manuscript stalls or a reviewer requests it.
- Formalize the (correct but informal) MI-IC v4.5 derivation as an appendix theorem, generalized to arbitrary regular-likelihood models.
- Optional citation backlog (White 2011, Grund 2021, Schomaker 2007) -- none load-bearing.
- PUBLIC-RELEASE OPEN LOOP (2026-06-28): the manuscript's transcripts link is HELD ("redacted copy in preparation") until a redacted `.claude` copy is shared -- then restore the link + unshare the raw `.claude`. Scrubber at `scripts/scrub_transcripts.py`.

(Resolved this session, moved to Decisions made: the FABLE DERIVATION VERDICT (option (i), scoped theorem + characterized Bayesian-draw term) and the amelia-vs-Bayesian-draw MECHANISM puzzle (bootstrap second-moment identity E*[Sigma~ + (mu~-muhat)^{x2}] = Sigmahat; the deflation IS the compensating -O(1/n) term conjectured) -- see FABLE AUDIT VERDICT + PROVENANCE ACCOUNTING. Resolved the prior session: the brms role (0b runtime spike) -> cross-check/non-normal-only (~24s/fit); `norm::da.norm` kept as a cross-check (per-rep rngseed, reproduces the conjugate -0.783). Carry-overs from prior sessions still open above: BIC cache check; non-normal family/congeniality; r3#3 cited method; sequencing; prose residuals; apaquarto render noise + second-render-target port; annotation discretionary items; real-data demo; MI-IC appendix theorem; citation backlog; public-release transcripts loop.)

(Resolved this session, 2026-07-02: whether the GPT-5.5 xhigh gate runs on Fable's new
derivations before they land -- it ran, end to end, gpt-5.5-pro via a newly-added Responses-API
path, verdict mixed and triaged; see the milestone note and the new open items above (IMB
closed-form verification; the C7 N-scan extension). Also corrected this session: the round-1
"IW -0.78 = 83% b_Sigma centering / 13% mu-wander / 4% imbalance" percentage decomposition,
which does not survive scrutiny -- see the milestone note's CORRECTION line, which supersedes
that specific claim in the FABLE AUDIT VERDICT decision without deleting it.)

(Resolved this session, 2026-07-02, continued: MARCUS'S FRAMING CALL -- ratified after a clean
Consensus.app due-diligence pass (three queries, one new candidate reference logged, nothing
contradicting or preempting found), and the amendment is landed (`decisions.md` D-22, commit
`2ed3f3a`, render-verified). Whether full RAG/citation-discipline diligence was done for the
landing -- checked directly rather than assumed: zero external citations in the landed text,
litrev's RAG confirmed down, file-RAG fallback confirmed all four already-owned provenance
papers support their attributed claims verbatim. Only remaining provenance item: Marcus
acquiring the 2-3 not-yet-owned references, now Next 3 actions #1.)
