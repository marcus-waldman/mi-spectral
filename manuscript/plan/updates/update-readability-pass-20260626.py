# Prose readability pass over the manuscript (S1-S6), 2026-06-26
#
# Milestone: the assembled PDF read above grade level and demanded inference.
# Directive (charter / CLAUDE.md prose-style): every paragraph must run the
# topic-sentence -> supporting-details -> wrap-up/hand-off arc, state each logical
# step plainly (a little redundancy is fine), and read at a lower grade level.
#
# WHAT THIS CHANGES: draft_prose only, for 67 of the 82 S1-S6 paragraphs. S3 (already
# the plainest section, FK ~8) and the cleanest S1/S2 paragraphs were left untouched.
# NO number, claim, hedge, citation, cross-reference, figure markup, or technical term
# was altered. The word "oracle" (pending a separate rename decision) was left verbatim
# everywhere it appears (S3-P6, S4-P4, S4-P7).
#
# METHOD: calibrated on S5-P12 (approved target: FK 13.2->8.6 on the body), then scaled.
# Each rewrite was checked by a mechanical guardrail that compares the multiset of math
# spans ($...$, $$...$$), citations/cross-refs (@ref, [@ref]), and figure markup before
# vs after -- all preserved on all 67. The CLAUDE.md prose punctuation gate
# (check_plan.prose_punctuation_hits) reports 0 hits. Every S5 rewrite (the freshly
# tribunal-amended section) was read line-by-line against its original for meaning drift;
# the scoped claims of S5-P15/P17/P18/P19/P20/P22 are preserved verbatim in meaning.
#
# MEASURED EFFECT (Flesch-Kincaid grade, LaTeX/citations stripped):
#   corpus S1-S6: 11.9 -> 10.2 ; per-section means all dropped.
#
# NOTE for the annotation milestone: review.topic_sentence / closing_sentence are left
# as the prior rhetorical-intent annotations; they are revisited in the per-paragraph
# annotation pass. Two open items flagged to Marcus: (1) S6-P4 ends with "**G6.**" (MVN
# limit) and S6-P4b is also "**G6.**" (congeniality) -- a provisional G-number collision
# the tribunal left for renumbering; (2) the conditioning-sense "oracle" rename.

import json

L3 = r"C:\Users\marcu\git-repositories\mi-spectral\manuscript\plan\level3-paragraphs.json"

FLAG = "readability-pass 2026-06-26: FK lowered, topic/support/wrap arc + plain statements; numbers, claims, hedges, citations, figure markup all preserved (mechanically verified)."

REWRITES = json.loads(r'''
{
 "S1-P2": "The exception is inference carried by the likelihood itself. For likelihood-ratio tests, a combining rule has existed since @mengPerformingLikelihoodRatio1992b. Its modern repairs give tests with accurate size [@chanMultipleImprovementsMultiple2022; @chanGeneralFeasibleTests2022]. For likelihood-based model selection, the picture is different. Here the literature's assessment of itself is that the question is open. Only one information criterion has been proposed for multiply imputed data. It closes by saying the method \"seems worthwhile to be further investigated, both theoretically as well as in practical situations\". That investigation lay beyond the paper's scope [@consentinoclaeskensOrderSelectionTests2010]. The first comprehensive study of model selection after multiple imputation calls the literature \"surprisingly sparse\" [@schomakerModelSelectionModel2014a]. The same study cautions that selecting by averaged criteria \"is not supported by MI literature.\" For variable selection, \"there are currently no guidelines\" [@woodHowShouldVariable2008a]. The applied guides are silent in the same direction. The standard book-length treatment does not treat information criteria for multiply imputed data at all [@vanbuurenFlexibleImputationMissing2018a]. Neither does the current state-of-the-art review [@endersMissingDataUpdate2025].",
 "S1-P3": "What, then, should \"settled\" mean? We propose a standard and organize this paper around it. We call it the *complete-data replication principle*. A procedure for multiply imputed data replicates complete-data inference under one condition. On average over repeated samples, it must reach the same conclusion that would have been reached had no data been missing. The principle can be demanded at three levels. At the first level, estimates recover their complete-data targets. This is the classical level, and Rubin's rules settle it. At the second level, the decision criterion itself recovers its complete-data counterpart in expectation. This criterion is a deviance or an information criterion. At the third level, decision rates match. The same model is selected, and the same hypotheses are rejected, as often as they would have been with complete data. Stated this way, the settled results are settled because they pass at the first level. The open questions above are open because the second and third levels had not been posed. Benchmarking selection methods against the full data is not new in itself. Simulation comparisons of that kind appear in @woodHowShouldVariable2008a and @consentinoclaeskensOrderSelectionTests2010. The principle as an explicit yardstick is, to our knowledge, stated here for the first time. We add a characterization of when it can and cannot be met.",
 "S1-P4": "The obstacle is a bias in the averaged log-likelihood. This paper's central result describes that bias exactly. Suppose a likelihood model with an estimated variance or covariance is fit to congenially imputed data. We show that the averaged log-likelihood then overstates its complete-data counterpart by\n$$ \\tfrac{1}{2}\\operatorname{tr}(\\mathrm{RIV}) \\;+\\; (A){+}(C). $$\nThe first term is one half the trace of the relative-increase-in-variance matrix. This matrix is a standard object in the MI literature. Its trace adds up the missing information about the model's parameters. The second term is smaller. It reflects imbalance between the observed and the missing units on the conditioning variables. It vanishes when the data are missing completely at random. The practical consequence is that every deviance and every information criterion computed from multiply imputed data is too optimistic. Worse, the optimism is not uniform across models. Each candidate's criterion is inflated in proportion to that candidate's own missing information. A model-comparison table built on imputed data therefore favors the candidates with the most missing information. In our pre-registered study, every misclassification by uncorrected MI-AIC chose the candidate with the largest RIV. The fix is direct. Add each candidate's own trace back to its criterion. We show that the corrected criterion recovers its complete-data expectation at any signal strength. This is the principle's second level, met in full.",
 "S1-P6": "These results sit upstream of the test-calibration literature and beside the model-selection one. The calibration line runs from @mengPerformingLikelihoodRatio1992b through @chanMultipleImprovementsMultiple2022 to @chanGeneralFeasibleTests2022. That line calibrates the reference distribution of an MI test statistic. It takes the statistic's numerator as given. We derive the bias of that numerator. The two are complementary. Calibration makes the reference distribution right. The present correction makes the statistic referred to it centered. As a penalty, the correction is precedented. It reproduces $\\mathrm{AIC}_{x;y}$ of @shimodairaInformationCriterionModel2017. That criterion halved the missing-data surcharge of the earlier complete-data-discrepancy criteria [@cavanaughAkaikeInformationCriterion1998a]. All of those criteria were derived for deterministic EM estimation under a fixed missingness pattern. Two other routes sit nearby. The missing-covariate criteria of @claeskensconsentinoVariableSelectionIncomplete2008 target a different, Takeuchi-type discrepancy. The reweighting route of @hensModelSelectionIncomplete2006a reaches the complete-data target through inverse-probability weights. It explicitly leaves an imputation-based criterion open. Five things here are new. First, the decomposition of the bias into an imputation-bias part and an estimation-mismatch part, with its estimated-scale scope condition. Second, the design-imbalance term $(A)+(C)$ under MAR, which lies beyond Shimodaira's fixed-pattern setting. His concluding section names the combination of missingness with other sampling mechanisms, such as covariate shift [@shimodairahidetoshiImprovingPredictiveInference2000], as future work. Third, the extension from the deterministic EM Q-function to proper multiple imputation. Fourth, the exact differential bias of the likelihood-ratio numerator in the complete-data metric. Fifth, the replication principle itself, with the null/noncentral characterization of its third level. Congeniality is assumed throughout [@mengMultipleImputationInferencesUncongenial1994a]. The bias derived here is what remains after the imputer and the analyst agree.",
 "S2-P2": "**Notation: two likelihoods, two estimators.** This paper turns on two likelihoods and two estimators, so we fix that notation first. The basic objects are a data matrix and a missingness indicator. Write $Y = (Y_{\\text{obs}}, Y_{\\text{mis}})$ for the complete data, split into its observed and missing parts. Write $R$ for the indicator of which entries are observed. A parametric model $f(Y \\mid \\theta)$ gives two likelihoods. The complete-data likelihood is $f(Y \\mid \\theta)$ itself. The observed-data likelihood integrates the missing part out, $f(Y_{\\text{obs}} \\mid \\theta) = \\int f(Y_{\\text{obs}}, Y_{\\text{mis}} \\mid \\theta)\\, dY_{\\text{mis}}$. Each likelihood has its own maximizer, and the two play different roles. The complete-data maximum likelihood estimate $\\hat\\theta_{\\text{com}}$ is computable only when nothing is missing. It is the target an analyst would have reached with full data. The observed-data maximum likelihood estimate $\\hat\\theta_{\\text{obs}}$ is the best that can be computed from the data at hand. The bias at the center of this paper compares likelihood values built around these two estimators. It measures what happens when values built around one estimator are read as if they were built around the other. @sec-derivations instantiates this notation for the multivariate normal model. Everything in this section is general.",
 "S2-P3": "**Ignorability.** This paper assumes throughout that the missingness mechanism is ignorable. The assumption and its conditions are Rubin's [@rubinInferenceMissingData1976b]. The data must be missing at random. This means the conditional probability of the observed missingness pattern is the same for all values of the missing data. The mechanism's parameter must also be distinct from the data parameter. These are the weakest general conditions for ignoring the mechanism in likelihood and Bayesian inference. Sampling-distribution inference is stricter. It requires in addition that the observed data are observed at random. That extra condition, together with MAR, amounts to MCAR. Sampling-distribution inference is otherwise generally conditional on the observed pattern. The gap between the two senses of \"ignorable\" is not a technicality for this paper. Two of its objects live inside that gap. The design-imbalance term $(A)+(C)$ of the main theorem is one. The information-matrix distinction restated at the end of this section is the other.",
 "S2-P4": "**The missing-information principle and the RIV.** The paper's central matrix is the relative-increase-in-variance matrix. It comes from the missing-information principle. @woodburyMissingInformationPrinciple1972 decompose the information in the complete data into the observed-data information plus what they call the lost information. They state this decomposition together with a score identity. The observed-data score is the conditional expectation of the complete-data score. @Meng01121991 state the same principle in the form used throughout this paper:\n$$ I_{\\text{obs}} \\;=\\; I_{\\text{com}} - I_{\\text{mis}\\mid\\text{obs}}, $$ {#eq-bg-mip}\nIn words, observed information equals complete information $-$ missing information. The central matrix is built from these ingredients:\n$$ \\mathrm{RIV} \\;=\\; I_{\\text{obs}}^{-1}\\, I_{\\text{mis}\\mid\\text{obs}}. $$ {#eq-bg-riv}\nIts trace adds up the odds of missing information about each parameter. It is the matrix form of Rubin's scalar relative increase in variance, restated below. One warning is needed here. The EM literature works with a different normalization of the same ingredients. The EM rate matrix $DM = I_{\\text{mis}\\mid\\text{obs}}\\, I_{\\text{com}}^{-1}$ of @dempsterMaximumLikelihoodIncomplete1977a and @Meng01121991. The RIV divides by the observed-data information. The rate matrix divides by the complete-data information. The two matrices have different eigenvalues. Conflating them corrupts every trace formula in this paper. The notation keeps them apart.",
 "S2-P5": "**EM and the Q-function.** The object whose bias this paper derives is a Q-function. So its definition and one geometric fact about it are needed. @dempsterMaximumLikelihoodIncomplete1977a define\n$$ Q(\\phi' \\mid \\phi) \\;=\\; E\\big(\\log f(\\mathbf{x} \\mid \\phi') \\,\\big|\\, \\mathbf{y}, \\phi\\big), $$ {#eq-bg-q}\nthe expected complete-data log-likelihood given the observed data. The E-step computes $Q(\\cdot \\mid \\phi^{(p)})$ and the M-step maximizes it. Their heuristic is direct. We do not know $\\log f(\\mathbf{x}\\mid\\phi)$, so we maximize instead its current expectation given the data $\\mathbf{y}$ and the current fit $\\phi^{(p)}$. Two facts from their analysis carry the weight later. First, the Q-function decomposes as $Q(\\phi'\\mid\\phi) = L(\\phi') + H(\\phi'\\mid\\phi)$. Here $L$ is the observed-data log-likelihood and $H$ is a conditional-entropy term. The main theorem is, at bottom, an account of what the $H$ term does when its parameters are estimated rather than known. Second, the curvature of $Q$ at its maximizer is the complete-data information $I_{\\text{com}}$, not the observed-data information. This single geometric fact drives the likelihood-ratio result. Constrained fits of the averaged log-likelihood project in the $I_{\\text{com}}$ metric.",
 "S2-P6": "**Imputation is Monte Carlo integration of the E-step.** The averaged log-likelihood over imputations is itself a Q-function, computed by Monte Carlo. @weiMonteCarloImplementation1990 implement the E-step by simulation. They draw $z^{(1)},\\dots,z^{(m)}$ from the conditional predictive distribution of the missing data. These draws replace the E-step integral by an average over completed data sets. Their Remark 2 makes the connection to multiple imputation explicit. They note that Rubin (1987) referred to the quantities $z^{(1)}, \\ldots, z^{(m)}$ as multiple imputations. One feature of this construction must be marked at once. The draws are taken at a *fixed* parameter value, the current iterate or in the limit the observed-data estimate. Imputation at a fixed parameter value is what Rubin calls *improper*. The $m \\to \\infty$ limit of the averaged log-likelihood is written $\\bar Q_\\infty(\\theta)$. It is the central object of this paper. The next paragraph states the form of imputation under which this paper studies it.",
 "S2-P7": "**Proper imputation.** The imputations this paper studies are proper in Rubin's sense. The distinction between proper and improper imputation does real work in the theorem. Proper imputation propagates parameter uncertainty. A parameter value is first drawn from its posterior given the observed data. The missing values are then drawn from the predictive distribution at that drawn value. Chapter 4 of @rubinMultipleImputationNonresponse1987 states the validity conditions. It gives the fully normal Bayesian scheme as the canonical example. Improper imputation skips the first draw and imputes at a fixed parameter value. The Monte Carlo E-step of the previous paragraph is improper by construction. So is the entire deterministic-EM lineage in which the earlier missing-data criteria were derived [@cavanaughAkaikeInformationCriterion1998a; @shimodairaInformationCriterionModel2017]. The distinction is not bookkeeping. The extra posterior draw contributes its own variation to the averaged log-likelihood. The main theorem prices it exactly. Consider the known-scale case. The bias is zero under improper imputation at the observed-data estimate. It is $-\\tfrac12\\operatorname{tr}(\\mathrm{RIV})$ under proper imputation. The two forms differ by precisely the posterior-draw contribution. Properness is also not absolute. @nielsenProperImproperMultiple2003 shows that Bayesian imputations are proper when the analyst's complete-data estimator is the maximum likelihood estimator. The same imputations can fail to be proper for a different estimator. This paper's analyst always uses the complete-data MLE. That is the case where congeniality implies properness [@mengMultipleImputationInferencesUncongenial1994a; @nielsenProperImproperMultiple2003]. The extension of the bias accounting from the improper, deterministic Q-function to proper multiple imputation is one of the things this paper adds.",
 "S2-P8": "**Rubin's rules are exact posterior-moment identities.** This paper uses Rubin's combining rules through the likelihood, not as moment formulas. That use requires them in their exact form. Result 3.2 of @rubinMultipleImputationNonresponse1987 shows two things. The posterior mean of an estimand given the observed data equals the average of the completed-data posterior means. The posterior variance equals the average completed-data variance plus the variance of the completed-data means. These are the ordinary rules for conditional moments, applied to imputation. With infinitely many imputations they are exact identities, not asymptotic approximations. In Rubin's notation they give $\\bar Q_\\infty$, $\\bar U_\\infty$, $B_\\infty$, and the total variance $T_\\infty = \\bar U_\\infty + B_\\infty$. Rubin then defines the scalar relative increase in variance due to nonresponse,\n$$ r_\\infty \\;=\\; B_\\infty / \\bar U_\\infty $$ {#eq-bg-rinf}\n(his equation 3.1.7). The RIV matrix of @eq-bg-riv is this quantity in matrix form. One approximation separates the exact identities from usable inference. It is \"the usual approximation of the posterior distribution as normal\", a Laplace-type approximation [@Tierney01031986]. The main theorem describes what happens when the exact moment identities are used through the likelihood itself, not as moments.",
 "S2-P9": "**Congeniality.** The second standing assumption is congeniality. It says the imputer and the analyst must agree. @mengMultipleImputationInferencesUncongenial1994a formalizes that agreement. An analysis procedure is congenial to an imputation model when one Bayesian model reproduces both. The posterior means and variances of that Bayesian model asymptotically match the analyst's complete-data and incomplete-data procedures. Its posterior predictive distribution for the missing data is the imputation model. Everything in this paper assumes congenial, proper imputation. This assumption is what gives the main theorem its force. The bias derived there is not an artifact of imputer-analyst disagreement. It is what remains after they agree.",
 "S2-P10": "**AIC is a bias-corrected plug-in estimate.** The model-selection application corrects AIC, so AIC's own logic is needed first. @akaikeNewLookStatistical1974a evaluates a fitted model by its mean log-likelihood against the true distribution. The maximized log-likelihood is the natural estimate of this criterion. That estimate is too optimistic. It \"needs a correction for the downward bias introduced by replacing $\\theta$ by $\\hat\\theta$. This correction is simply realized by adding $k$,\" the parameter count. The result is\n$$ \\mathrm{AIC} \\;=\\; -2\\log(\\text{maximum likelihood}) + 2k. $$ {#eq-bg-aic}\nThe model-selection application repeats this accounting with one more bias source. Under multiple imputation the goodness-of-fit term is $-2\\bar Q_\\infty$ rather than the complete-data deviance. That added bias is exactly what the main theorem quantifies.",
 "S2-P11": "**Wilks.** The complete-data baseline for testing is Wilks' theorem. The replication principle is defined against it. Consider a null hypothesis that fixes $h - m$ of $h$ parameters. @wilksLargeSampleDistributionLikelihood1938a shows that $-2\\log\\lambda$ is distributed as $\\chi^2_{h-m}$ in large samples. Every multiply imputed deviance in this paper is ultimately compared against this reference. The complete-data replication principle asks when that comparison behaves as it would have with full data.",
 "S2-P12": "**Observed versus expected information under MAR.** One convention must be fixed before any trace in this paper is computed. The question is which information matrix to use under MAR. @kenwardLikelihoodBasedFrequentist1998 settle it. Under MAR the missingness indicator is not ancillary. The correct sampling framework is therefore unconditional over both the data and the missingness pattern. A \"naive\" expected information is computed as if the realized pattern were fixed by design, and it is biased. MCAR is necessary and sufficient for the naive and unconditional forms to agree. Their recommendation is to use the observed information, and this paper follows it. Their bivariate Gaussian example shows where the difference lodges. Under MAR dropout, the unconditional information acquires mean-covariance cross terms that the naive form misses. Both facts recur in @sec-derivations. The design-imbalance term $(A)+(C)$ is nonzero exactly when MCAR fails, and it is computed against the observed information. The observed-data information behind the one RIV of this paper carries exactly their MAR cross term.",
 "S2-P13": "**MI test combining, calibration, and the prior MI-AIC.** Three strands of prior work meet the applications directly, and each is restated here as the launch point it provides. The first strand is the combining rule. @mengPerformingLikelihoodRatio1992b combine complete-data likelihood-ratio statistics across imputations into a single test statistic. They calibrate it against an $F$ reference under an equal-fractions assumption. @chanMultipleImprovementsMultiple2022 repair the procedure's known defects by switching the order of operations. Their statistic\n$$ \\hat d_{\\mathrm{L}} \\;=\\; 2\\big\\{\\bar L(\\hat\\psi^{*}) - \\bar L(\\hat\\psi_0^{*})\\big\\}, \\qquad \\hat\\psi^{*} = \\arg\\max_\\psi \\bar L(\\psi), $$ {#eq-bg-dl}\nmaximizes the averaged log-likelihood rather than averaging the maxima. It is nonnegative and invariant by construction. The numerator analyzed in @sec-derivations is exactly this maximize-then-average statistic. The second strand is the reference distribution. @chanGeneralFeasibleTests2022 drops the equal-fractions assumption entirely. Stacking the imputed data sets yields estimators of every eigenvalue $r_j$ of the odds-of-missing-information matrix. The limiting null law of the combined statistic is a weighted sum. Its mean exceeds the parameter count by the total odds of missing information in the tested directions. That excess matters later. A reference built this way absorbs the corresponding bias in the numerator, so the bias bears on procedures that use no such reference. The third strand is the criterion. @consentinoclaeskensOrderSelectionTests2010 propose an AIC for multiply imputed data by attaching the Meng-Rubin combined statistic to the standard penalty. Their criterion does not analyze the bias of the averaged log-likelihood, and their closing assessment leaves the theory open. The corrected criterion of @sec-derivations is the answer to the question their proposal poses.",
 "S4-P2": "## Setup and main theorem\n\nWe state the theorem for a general regular likelihood that estimates a scale or covariance. We then instantiate it in the multivariate normal family. Let $Y \\in \\mathbb{R}^{N \\times p}$ collect $N$ independent rows $Y_i \\sim \\mathcal{N}_p(\\mu, \\Sigma)$ with estimand $\\theta = (\\mu, \\operatorname{vech}\\Sigma)$ of dimension $k = p + \\tfrac12 p(p+1)$. Missingness is ignorable throughout. The two estimators of @sec-background take concrete form here. The complete-data maximizer $\\hat\\theta_{\\text{com}}$ is the target an analyst with full data would have reached. The observed-data maximizer $\\hat\\theta_{\\text{obs}}$ sums each row's marginal density over its observed coordinates [@schaferAnalysisIncompleteMultivariate1997]. It is the best available from the data at hand. We take up the scope of the normal instantiation in @sec-discussion.",
 "S4-P3": "We now instantiate the RIV of @eq-bg-riv. One convention is fixed and one warning is repeated. In this family\n$$ \\mathrm{RIV} \\;=\\; I_{\\text{obs}}^{-1}\\, I_{\\text{mis}\\mid\\text{obs}} \\;=\\; I_{\\text{obs}}^{-1} I_{\\text{com}} - I_k, \\qquad \\operatorname{tr}(\\mathrm{RIV}) = \\operatorname{tr}(I_{\\text{obs}}^{-1} I_{\\text{com}}) - k. $$ {#eq-riv}\nUnder congenial proper MI with $m \\to \\infty$, Rubin's rules give $\\bar U_\\infty = I_{\\text{com}}^{-1}$ and $T_\\infty = I_{\\text{obs}}^{-1}$. So this matrix is exactly Rubin's $r_\\infty$ in matrix form. The convention is fixed first. Under MAR the expectation defining $I_{\\text{obs}}$ is taken jointly over data and pattern, without conditioning, as @kenwardLikelihoodBasedFrequentist1998 require. The resulting information carries their mean-covariance cross term. Computed this way there is a single RIV, and both bias terms of the theorem calibrate to it. An earlier reading of our own simulation output made the two terms appear to attach to two different RIVs. That reading was an artifact of grading against the naive block-diagonal information. The warning repeats @sec-background in local form. The fraction-of-missing-information matrix $D = I_{\\text{com}}^{-1} I_{\\text{mis}\\mid\\text{obs}}$ [@schaferAnalysisIncompleteMultivariate1997] has the same trace ingredients but the other normalization. The two relate by $D = (I_k + \\mathrm{RIV})^{-1}\\mathrm{RIV}$. Substituting one for the other changes every constant below.",
 "S4-P4": "The object of the theorem is the infinite-imputation Q-function. Its definition contains a choice that fixes the sign of everything that follows. Under congenial proper MI the imputation parameter is a posterior draw, $\\tilde\\phi \\sim \\pi(\\phi \\mid Y_{\\text{obs}})$. Here $\\pi$ is the posterior of the imputation model. Under congeniality (R4) that is the model whose predictive distribution the analyst's procedure agrees with. Under deterministic FIML imputation the draw degenerates to the point mass $\\tilde\\phi = \\hat\\theta_{\\text{obs}}$. In either case $\\tilde\\phi$ is centered at $\\hat\\theta_{\\text{obs}}$ with $\\operatorname{Var}(\\tilde\\phi) = I_{\\text{obs}}^{-1} + O(n^{-2})$. The order counting below draws on this property. Each completion is drawn at $\\tilde\\phi$. The infinite-imputation Q-function averages these completions.\n$$ \\bar Q_\\infty(\\theta) \\;:=\\; \\lim_{M\\to\\infty} \\frac1M \\sum_{l=1}^{M} \\ell_{\\text{com}}\\big(\\theta;\\, Y_{\\text{obs}}, \\tilde Y_{\\text{mis}}^{(l)}\\big) \\;=\\; \\mathbb{E}_{\\tilde\\phi}\\, \\mathbb{E}_{Y_{\\text{mis}} \\mid Y_{\\text{obs}}, \\tilde\\phi}\\big[\\ell_{\\text{com}}(\\theta)\\big]. $$ {#eq-qbar}\nThe inner expectation conditions on the fitted or drawn parameter, exactly as the EM E-step conditions on the current iterate. It does not condition on the truth. This is the fork that @sec-methods reported as the sign episode. Both branches are stated here to prevent the standard confusion. Were $\\bar Q_\\infty$ instead the oracle expectation $\\mathbb{E}[\\ell_{\\text{com}}(\\theta) \\mid Y_{\\text{obs}}]$ under the data-generating law, the tower property would force the imputation-bias term to zero and the total bias would be $-\\tfrac12\\operatorname{tr}(\\mathrm{RIV})$, with the opposite sign. The analyst imputes from a model fit to the data, not from an oracle. The two regimes are distinguished experimentally by a dedicated study in @sec-simulations.",
 "S4-P5": "The bias is split at the pivot $\\hat\\theta_{\\text{obs}}$ into two terms with distinct mechanisms,\n$$ T \\;=\\; \\underbrace{\\big[\\bar Q_\\infty(\\hat\\theta_{\\text{obs}}) - \\ell_{\\text{com}}(\\hat\\theta_{\\text{obs}})\\big]}_{T_{\\text{imp}}:\\ \\text{imputation-bias term}} \\;+\\; \\underbrace{\\big[\\ell_{\\text{com}}(\\hat\\theta_{\\text{obs}}) - \\ell_{\\text{com}}(\\hat\\theta_{\\text{com}})\\big]}_{T_{\\text{est}}:\\ \\text{estimation-mismatch term}}, $$ {#eq-split}\non the log-likelihood scale. Deviance-scale statements double everything. The companion derivation calls these Term A and Term B. The descriptive subscripts are used here instead. The companion's bookkeeping also uses the labels $(A)$ and $(C)$ for pieces inside the first term. Those letters must not collide. Seven regularity conditions are in force and none is exotic. R1 and R2 are smoothness and positive-definite information [@vaartAsymptoticStatistics1998a]. R3 is ignorability [@rubinInferenceMissingData1976b]. R4 through R6 are congeniality, properness, and self-efficiency [@mengMultipleImputationInferencesUncongenial1994a; @rubinMultipleImputationNonresponse1987]. R7 is the infinite-imputation idealization. Rubin's finite-$m$ corrections apply otherwise. Together they deliver the variance-recovery property $T_\\infty = I_{\\text{obs}}^{-1}$ that identifies @eq-riv with $r_\\infty$.",
 "S4-P6": "::: {#thm-bias}\n## Q-function deviance bias\n\nUnder R1-R7, for a model that estimates a scale or covariance, to leading order\n$$ \\mathbb{E}[T] \\;=\\; +\\tfrac12\\operatorname{tr}(\\mathrm{RIV}) \\;+\\; \\big[(A)+(C)\\big] \\;+\\; O(n^{-1}), $$ {#eq-thm}\ndecomposing as $\\mathbb{E}[T_{\\text{imp}}] = +\\operatorname{tr}(\\mathrm{RIV}) + [(A)+(C)] + O(n^{-1})$ and $\\mathbb{E}[T_{\\text{est}}] = -\\tfrac12\\operatorname{tr}(\\mathrm{RIV}) + O(n^{-1})$. The design-imbalance term $(A)+(C)$ vanishes under MCAR and is a nonzero $O(1)$ under MAR.\n:::\n\nThe theorem is firm in the sense of @sec-methods. The two terms are derived in turn. The estimation-mismatch term comes first. We expand $\\ell_{\\text{com}}$ to second order about its own maximizer. The score vanishes there, giving\n$$ T_{\\text{est}} \\;=\\; -\\tfrac12\\,(\\hat\\theta_{\\text{obs}} - \\hat\\theta_{\\text{com}})^\\top \\mathcal{J}_{\\text{com}}(\\hat\\theta_{\\text{com}})\\,(\\hat\\theta_{\\text{obs}} - \\hat\\theta_{\\text{com}}) + O_p(n^{-3/2}). $$\nWe then take expectations, replace the realized curvature by $I_{\\text{com}}$, and write the quadratic form as a trace. This gives\n$$ \\mathbb{E}[T_{\\text{est}}] \\;=\\; -\\tfrac12\\operatorname{tr}\\!\\big(I_{\\text{com}}\\operatorname{Var}(\\hat\\theta_{\\text{obs}} - \\hat\\theta_{\\text{com}})\\big) + O(n^{-3/2}). $$\nThe variance of the gap is the substantive step. It runs through the score identity. Differentiating the factorization of @eq-split's first term and taking conditional expectations gives $U_{\\text{obs}} = \\mathbb{E}[U_{\\text{com}} \\mid Y_{\\text{obs}}]$. From this,\n$$ \\operatorname{Cov}(U_{\\text{obs}}, U_{\\text{com}}) = \\operatorname{Var}(U_{\\text{obs}}) = I_{\\text{obs}}, \\qquad \\operatorname{Cov}(\\hat\\theta_{\\text{obs}}, \\hat\\theta_{\\text{com}}) = I_{\\text{obs}}^{-1}\\, I_{\\text{obs}}\\, I_{\\text{com}}^{-1} = I_{\\text{com}}^{-1}, $$\nthe second equality holding by the asymptotic linearity of both estimators. An equivalent statement is that the complete-data estimator is asymptotically uncorrelated with the gap. Assembling the three pieces of the variance,\n$$ \\operatorname{Var}\\big(\\hat\\theta_{\\text{obs}} - \\hat\\theta_{\\text{com}}\\big) \\;=\\; I_{\\text{obs}}^{-1} + I_{\\text{com}}^{-1} - 2I_{\\text{com}}^{-1} \\;=\\; I_{\\text{obs}}^{-1} - I_{\\text{com}}^{-1}, $$ {#eq-vargap}\nand substituting back,\n$$ \\mathbb{E}[T_{\\text{est}}] \\;=\\; -\\tfrac12\\operatorname{tr}\\!\\big(I_{\\text{com}} I_{\\text{obs}}^{-1} - I_k\\big) \\;=\\; -\\tfrac12\\big(\\operatorname{tr}(I_{\\text{obs}}^{-1} I_{\\text{com}}) - k\\big) \\;=\\; -\\tfrac12\\operatorname{tr}(\\mathrm{RIV}). $$\nOne trap is worth naming. Substituting $\\operatorname{Var}(\\hat\\theta_{\\text{obs}})$ for the variance of the gap in the second display produces a spurious $-\\tfrac12 k$. We display @eq-vargap to forestall it.",
 "S4-P7": "The imputation-bias term is the delicate half. Its derivation has three steps. First, factor the complete-data log-likelihood as $\\ell_{\\text{com}}(\\theta) = \\ell_{\\text{obs}}(\\theta) + \\ell_{\\text{mis}\\mid\\text{obs}}(\\theta)$ with $\\ell_{\\text{mis}\\mid\\text{obs}}(\\theta) := \\log P(Y_{\\text{mis}} \\mid Y_{\\text{obs}}, \\theta)$. The observed parts cancel inside $T_{\\text{imp}}$, leaving\n$$ T_{\\text{imp}} \\;=\\; \\mathbb{E}_{Y_{\\text{mis}}\\mid Y_{\\text{obs}},\\tilde\\phi}\\big[\\ell_{\\text{mis}\\mid\\text{obs}}(\\hat\\theta_{\\text{obs}})\\big] - \\ell_{\\text{mis}\\mid\\text{obs}}(\\hat\\theta_{\\text{obs}}). $$\nSecond, take expectations over the true completion. What remains is the discrepancy between averaging the missing-data log-density under the fitted $\\tilde\\phi$ and averaging it under the truth.\n$$ \\mathbb{E}[T_{\\text{imp}}] \\;=\\; \\mathbb{E}_{Y_{\\text{obs}}}\\big[\\,\\mathbb{E}_{\\tilde\\phi}[\\ell_{\\text{mis}\\mid\\text{obs}}(\\hat\\theta_{\\text{obs}})] - \\mathbb{E}_{\\theta_0}[\\ell_{\\text{mis}\\mid\\text{obs}}(\\hat\\theta_{\\text{obs}})]\\,\\big]. $$\nThese two inner expectations would cancel were $\\tilde\\phi = \\theta_0$, which is the oracle case of @eq-qbar and the tower-property branch stated there. The term is nonzero precisely because the imputations use $\\tilde\\phi \\approx \\hat\\theta_{\\text{obs}} \\neq \\theta_0$. Third, expand to second order in $\\tilde\\phi$ about $\\theta_0$, differentiating under the integral and using the Bartlett identity $\\mathbb{E}_{\\theta_0}[S_{\\text{mis}\\mid\\text{obs}}S_{\\text{mis}\\mid\\text{obs}}^\\top] = I_{\\text{mis}\\mid\\text{obs}}$. Three pieces result:\n$$ \\mathbb{E}[T_{\\text{imp}}] \\;=\\; \\underbrace{\\mathbb{E}\\big[(\\tilde\\phi-\\theta_0)^\\top\\alpha\\big]}_{(A)} \\;+\\; \\underbrace{\\mathbb{E}\\big[(\\tilde\\phi-\\theta_0)^\\top I_{\\text{mis}\\mid\\text{obs}}(\\hat\\theta_{\\text{obs}}-\\theta_0)\\big]}_{\\text{main cross term}} \\;+\\; \\underbrace{\\tfrac12\\mathbb{E}\\big[(\\tilde\\phi-\\theta_0)^\\top H_{\\phi\\phi}(\\tilde\\phi-\\theta_0)\\big]}_{(C)}. $$ {#eq-timp-pieces}\nHere $\\alpha = \\operatorname{Cov}_{\\theta_0}\\big(\\ell_{\\text{mis}\\mid\\text{obs}}, S_{\\text{mis}\\mid\\text{obs}}\\big) = -\\mathcal{E}'(\\theta_0)$ is the gradient of the conditional missing-data entropy. The term $H_{\\phi\\phi}$ is the curvature of the conditional cross-entropy. The labels $(A)$ and $(C)$ follow the companion derivation's bookkeeping. The companion letters its main piece $(B)$. This paper avoids that label for obvious reasons. The entropy gradient is not zero. Gibbs' inequality concerns varying the evaluation point of $\\mathbb{E}_{\\theta_0}[\\log p_\\theta]$. By contrast, $\\alpha$ varies the sampling distribution of the completions. The two are different functions. For the normal family the gradient is supported entirely on the covariance parameters. In the univariate case $\\alpha_\\mu = 0$ and $\\alpha_{\\sigma^2} = -n_{\\text{mis}}/2\\sigma^2 \\neq 0$. The main cross term reduces directly. Proper MI centers $\\tilde\\phi$ at $\\hat\\theta_{\\text{obs}}$, so\n$$ \\mathbb{E}\\big[(\\tilde\\phi-\\theta_0)^\\top I_{\\text{mis}\\mid\\text{obs}}(\\hat\\theta_{\\text{obs}}-\\theta_0)\\big] \\;=\\; \\operatorname{tr}\\!\\big(I_{\\text{mis}\\mid\\text{obs}}\\, \\operatorname{Cov}(\\tilde\\phi, \\hat\\theta_{\\text{obs}})\\big) \\;=\\; \\operatorname{tr}\\!\\big(I_{\\text{mis}\\mid\\text{obs}}\\, I_{\\text{obs}}^{-1}\\big) + O(n^{-1}) \\;=\\; \\operatorname{tr}(\\mathrm{RIV}) + O(n^{-1}), $$\nthe leading constant of the theorem.",
 "S4-P7b": "The two remaining pieces of @eq-timp-pieces form the design-imbalance term. Their orders are counted directly. Under proper MI centered at $\\hat\\theta_{\\text{obs}}$, the displacement $\\delta := \\tilde\\phi - \\theta_0$ has $\\mathbb{E}[\\delta] = O(n^{-1})$, the MLE bias, and $\\operatorname{Var}(\\delta) = I_{\\text{obs}}^{-1} = O(n^{-1})$. The factors $\\alpha$ and $H_{\\phi\\phi}$ are both $O(n)$, extensive in $n_{\\text{mis}}$. Hence\n$$ (A) = \\mathbb{E}[\\delta]^\\top\\alpha = O(1), \\qquad (C) = \\tfrac12\\operatorname{tr}\\!\\big(H_{\\phi\\phi}\\, I_{\\text{obs}}^{-1}\\big) + O(n^{-1}) = O(1), $$\ntwo order-one pieces of opposite sign. Their leading parts cancel exactly when the missing and observed units share a conditioning-variable distribution. That case is MCAR. Under MAR the sum survives. It has the bivariate monotone closed form\n$$ (A)+(C) \\;=\\; \\frac{n_{\\text{mis}}}{n_{\\text{obs}}} \\Big[\\, 1 - \\tfrac12 \\operatorname{tr}\\big(Q_{\\text{mis}} Q_{\\text{obs}}^{-1}\\big) \\Big] + O(n^{-1}), $$ {#eq-ac}\nwhere $Q_{\\text{mis}}$ and $Q_{\\text{obs}}$ are the conditioning-variable second-moment matrices of the missing and observed units. The term is zero exactly when the two distributions agree. For non-monotone patterns the covariance-MLE bias entering $(A)$ comes from the general second-order Cox-Snell bias of the MLE. We carry this bias to general dimension and verify it in two computer-algebra systems. Summing the pieces of @eq-timp-pieces with $\\mathbb{E}[T_{\\text{est}}]$ gives @eq-thm. These statements do not all rest on the same footing. The distinction matters for how the term is used. The structural facts are firm in the sense of @sec-methods. Its sign, its $O(1)$ order under MAR, its exact vanishing under MCAR, the closed form @eq-ac, and the information convention it is computed in are all proved. Its absolute magnitude is another matter. The per-replicate statistic is heavy-tailed. Direct Monte Carlo estimates therefore scatter without trend around the analytic leading-order value. The magnitude is measured only imprecisely. The saving fact is that comparisons never depend on the magnitude alone. Candidates fit to the same imputations touch $(A)+(C)$ only through their difference. The heavy-tailed component cancels in that difference. The comparison-relevant differential is derived and measured precisely in the next part.",
 "S4-P8": "Two checks close the theorem, one on its scope and one on its form. The scope check is the known-scale collapse. With a known scale and a location-only fit the conditional missing-data entropy is parameter-free. The net bias then collapses to\n$$ \\mathbb{E}[T]_{\\text{known scale}} \\;=\\; \\begin{cases} 0 & \\text{deterministic FIML}, \\\\[2pt] -\\tfrac12\\operatorname{tr}(\\mathrm{RIV}) & \\text{proper MI}, \\end{cases} $$ {#eq-knownscale}\nThe two arms differ by exactly the posterior-draw imputation variance of @sec-background's proper-imputation paragraph. The four-way collapse is a pre-registered prediction, tested in @sec-simulations. Every model compared below estimates a covariance. The applications therefore sit in the estimated-scale regime. The form check is the entropy-plug-in reading. Write $C_n(\\theta)$ for the conditional entropy of the missing block given the observed block. The bias @eq-thm equals the plug-in bias of evaluating $C_n$ at the estimate rather than the truth. One curvature identity regroups the proof's three pieces into a single expansion,\n$$ \\nabla^2 C_n(\\theta_0) \\;=\\; H_{\\phi\\phi} + I_{\\text{mis}\\mid\\text{obs}}, $$ {#eq-entropy-hessian}\nand delivers the known-scale collapse as the parameter-free special case. The total is convention-free. Both readings are verified symbolically. The theorem's pre-registered Monte Carlo confirmation is reported in @sec-simulations.",
 "S4-P10": "::: {#prp-lrt}\n## Differential bias at the null\n\nUnder R1-R7 the following holds at the null.\n$$ \\mathbb{E}\\big[\\hat d_L - \\hat d_{\\text{com}}\\big] \\;=\\; \\operatorname{tr}(\\mathrm{RIV}_\\perp) + O(n^{-1}), \\qquad \\operatorname{tr}(\\mathrm{RIV}_\\perp) \\;:=\\; \\operatorname{tr}\\big(I_{\\text{obs}}^{-1} I_{\\text{mis}\\mid\\text{obs}}\\big) - \\operatorname{tr}\\big[(G^\\top I_{\\text{com}} G)^{-1}\\, G^\\top I_{\\text{com}} I_{\\text{obs}}^{-1} I_{\\text{mis}\\mid\\text{obs}}\\, G\\big], $$ {#eq-lrt-bias}\nThis is the missing information of the tested directions, projected onto the null tangent space in the $I_{\\text{com}}$ metric.\n:::\n\nThis result is firm. The metric is its substance. Write $Z = \\nabla\\bar Q_\\infty(\\theta_0)$ for the score of the averaged log-likelihood at the truth. Write $J = -\\nabla^2\\bar Q_\\infty(\\theta_0)$ for its curvature. Because the conditional density integrates to one, the evaluation-slot gradient of the imputed part vanishes at $(\\theta_0, \\theta_0)$. The score then reduces to\n$$ Z \\;=\\; S + I_{\\text{mis}\\mid\\text{obs}}\\,(\\hat\\theta_{\\text{obs}} - \\theta_0) + O_p(1) \\;=\\; I_{\\text{com}}\\, I_{\\text{obs}}^{-1}\\, S + O_p(1), \\qquad \\operatorname{Var}(Z) \\;=\\; I_{\\text{com}}\\, I_{\\text{obs}}^{-1}\\, I_{\\text{com}} + O(\\sqrt n), $$\nwith $S$ the observed-data score. The curvature converges to\n$$ J \\;\\to_p\\; I_{\\text{obs}} + I_{\\text{mis}\\mid\\text{obs}} \\;=\\; I_{\\text{com}}, $$\nthe EM identity restated in @sec-background. The two limits are consistent only together. The unconstrained maximizer of $\\bar Q_\\infty$ must reproduce $\\hat\\theta_{\\text{obs}}$, which is EM self-consistency. Indeed $J^{-1} Z = I_{\\text{com}}^{-1}\\, I_{\\text{com}}\\, I_{\\text{obs}}^{-1} S = \\hat\\theta_{\\text{obs}} - \\theta_0$ holds only for $J = I_{\\text{com}}$. The constrained maximizer therefore projects $Z$ onto $\\operatorname{col}(G)$ in the $I_{\\text{com}}$ metric. The deviance is then the standard difference of quadratic forms,\n$$ \\hat d_L \\;=\\; Z^\\top\\big[I_{\\text{com}}^{-1} - G(G^\\top I_{\\text{com}} G)^{-1} G^\\top\\big] Z + O_p(n^{-1/2}). $$\nTaking expectations against $\\operatorname{Var}(Z)$ and subtracting Wilks' $\\mathbb{E}[\\hat d_{\\text{com}}] = q_{\\mathrm{d}} + O(n^{-1})$ gives @eq-lrt-bias. The natural error is now visible. Substituting $I_{\\text{obs}}$ for the curvature reproduces exactly the naive difference that the next proposition shows always overstates. That substitution is the same as conflating $\\hat\\psi_0^{*}$ with the null model's own observed-data MLE. The derivation was carried through three independent routes. A pre-registered design built to separate the two formulas is graded in @sec-simulations.",
 "S4-P11": "::: {#prp-naive}\n## The naive difference always overstates\n\nLet $\\operatorname{tr}(\\mathrm{RIV}_0) = \\operatorname{tr}[(G^\\top I_{\\text{obs}} G)^{-1} G^\\top I_{\\text{mis}\\mid\\text{obs}} G]$ be the null model's own self-contained trace correction. Then always\n$$ \\operatorname{tr}(\\mathrm{RIV}_\\perp) \\;\\le\\; \\operatorname{tr}(\\mathrm{RIV}) - \\operatorname{tr}(\\mathrm{RIV}_0), $$ {#eq-overstate}\nwith equality if and only if $\\operatorname{col}(I_{\\text{obs}}^{1/2} G)$ is an invariant subspace of the standardized missing information $H = I_{\\text{obs}}^{-1/2} I_{\\text{mis}\\mid\\text{obs}} I_{\\text{obs}}^{-1/2}$. Equal fractions of missing information is a special case of equality, with both sides equal to $r\\, q_{\\mathrm{d}}$ for the common odds $r$.\n:::\n\nThis result is firm. The proof is one matrix inequality. The Gram matrix of the pair $(I_{\\text{obs}}^{1/2} G,\\; I_{\\text{obs}}^{-1/2} I_{\\text{com}} G)$ is positive semidefinite. Its Schur complement gives\n$$ G^\\top I_{\\text{com}}\\, I_{\\text{obs}}^{-1}\\, I_{\\text{com}}\\, G \\;\\succeq\\; (G^\\top I_{\\text{com}} G)\\,(G^\\top I_{\\text{obs}} G)^{-1}\\,(G^\\top I_{\\text{com}} G), $$\nTracing both sides against $(G^\\top I_{\\text{com}} G)^{-1}$ yields @eq-overstate. The gap also has an exact closed form. That form makes the equality condition transparent. Partition $H$ into a block $H_{11}$ on the retained directions $\\operatorname{col}(I_{\\text{obs}}^{1/2} G)$, a block on the tested directions, and a coupling block $H_{12}$. This partition gives\n$$ \\big[\\operatorname{tr}(\\mathrm{RIV}) - \\operatorname{tr}(\\mathrm{RIV}_0)\\big] - \\operatorname{tr}(\\mathrm{RIV}_\\perp) \\;=\\; \\operatorname{tr}\\big[(I + H_{11})^{-1} H_{12} H_{12}^\\top\\big] \\;\\ge\\; 0, $$ {#eq-gap}\nThe gap is zero exactly when $H_{12} = 0$. That is when the tested and retained directions carry independent missing information. The overstatement is therefore precisely the missing-information coupling between the two subspaces. The retained block's own information screens it. The practical reading is direct. Correcting an MI deviance comparison by the difference of the two models' own traces over-corrects at the null. This happens whenever the tested directions mix unequally-missing information. The over-correction is negligible when the design sits near the invariance case. It grows to multiples when the design does not. Both regimes are exhibited in @sec-simulations.",
 "S4-P12": "::: {#prp-pairing}\n## Pairing collapses the noise\n\nLet $D$ be the per-dataset paired differential. At the null and under local alternatives, $\\operatorname{sd}(D) = O(1)$, against $O(\\sqrt n)$ for either level separately. At a fixed alternative the cancellation fails and $\\operatorname{sd}(D)$ reverts to $O(\\sqrt n)$.\n:::\n\nThis result is firm. The mechanism is exact cancellation of realizations rather than averaging. The large noise of each level lives in fit-independent realized constants. Both fits maximize the same realized $\\bar Q_\\infty$, and they build it from the same imputation parameter. So these constants are identical in the two levels. They cancel dataset by dataset. The simulations in @sec-simulations show both effects across sample sizes. The single-model noise grows. The paired differential stays flat. This is why the paper estimates every comparison-relevant quantity by paired contrasts. It never differences separately estimated levels.",
 "S4-P13": "::: {#prp-ac-diff}\n## The design-imbalance differential cancels at the null\n\nThe $(A)+(C)$ contributions of the two levels are properties of the imputation, not of which model is fit. At the null they are identical realizations and cancel exactly to leading order. Under local alternatives the differential is $O(n^{-1/2})$. It becomes a genuine $O(1)$ only when the candidates' pseudo-true values are separated at $O(1)$. That is the regime of @vuongLikelihoodRatioTests1989.\n:::\n\nSeparation, not nesting, is the criterion. A nested but false restriction triggers the decoupling just as a non-nested pair does. The expansion leads us to expect that the differential's size scales with how differently the competitors handle the missing data. The reason is that similar candidates respond to the same imbalance almost identically, and so their contributions nearly cancel. One caveat is analytic and travels with dissimilar pairs. A badly misspecified candidate also carries a mechanism-independent misspecification $O(1)$. An MCAR contrast separates this term from the design-imbalance term in measurement. But it can still offset part of the design-imbalance term in the net ranking bias. A pre-registered measurement covered all three. It covered the decoupling, the dissimilarity scaling, and the caveat. @sec-simulations reports it.",
 "S4-P14": "The last result of this part locates which procedures the differential bias actually affects. At the null, @eq-lrt-bias is precisely the mean inflation that a correctly calibrated reference distribution already absorbs. The limiting law of $\\hat d_L$ is the weighted sum $\\sum_{j} \\lambda_j \\chi^2_1$ with $\\lambda_j = 1 + r_{\\perp,j}$ [@chanGeneralFeasibleTests2022]. Their sum has a fixed value.\n$$ \\sum_{j=1}^{q_{\\mathrm{d}}} \\lambda_j \\;=\\; q_{\\mathrm{d}} + \\operatorname{tr}(\\mathrm{RIV}_\\perp). $$ {#eq-spectrum-sum}\nA test that refers the uncorrected numerator to such a reference is therefore approximately calibrated at the null. Correcting the numerator on top of it double-counts. We check that prediction against pre-registered null rates in @sec-simulations. The differential bias matters instead exactly where no reference distribution stands between the statistic and its use. Three procedures qualify. Information-criterion ranking compares penalized values directly. Explicit numerator corrections must use @eq-lrt-bias rather than the naive difference. Non-nested comparison brings back both the design-imbalance differential and the heavy-tailed noise. The first of these is taken up next.",
 "S4-P15": "A scope statement closes this part. It is structural in the sense of @sec-methods. The propositions above are proved for the deterministic-FIML $\\bar Q_\\infty$. Under proper MI the imputation-side quantities acquire posterior smearing. But they remain common to both fits, the same posterior and the same draws. So the cancellation structure and the leading-order form carry over structurally. This carry-over is argued, not separately measured. We list it as a limitation in @sec-discussion. The cross-model gate of @sec-methods was run on this part in full. A blind re-derivation reproduced the $I_{\\text{com}}$ metric, @eq-lrt-bias term by term, the definite sign of @eq-overstate with a third independent proof, and the noise orders. An adversarial pass instructed to break each claim sustained all four.",
 "S4-P16": "## Information-criterion selection\n\nThis part takes up the replication principle's third level for selection. Here the bias has no reference distribution to hide behind. Model selection by information criterion compares penalized deviances as numbers and takes the smallest. Whatever bias each candidate's $-2\\bar Q_\\infty$ carries lands directly in the ranking. By @thm-bias the bias is model-specific. The reason is that $\\mathrm{RIV}_k$ is computed on candidate $k$'s own parameter space. The corrected criterion takes this form.\n$$ \\mathrm{AIC}_{\\mathrm{MI}}^{c}(k) \\;=\\; -2\\bar Q_\\infty(\\hat\\psi^{*}_k) + 2q_k + \\operatorname{tr}(\\mathrm{RIV}_k). $$ {#eq-ic-corrected}\nThis penalty reproduces $\\mathrm{AIC}_{x;y}$ of @shimodairaInformationCriterionModel2017. Its missing-data surcharge is exactly half that of the earlier complete-data-discrepancy criteria [@cavanaughAkaikeInformationCriterion1998a]. What @thm-bias adds is the anatomy, the MAR term, and the proper-MI scope. The ranking consequence is immediate. For two candidates the uncorrected difference carries a differential bias of exactly the surplus of missing information. So the candidate with more missing information about its own parameters looks artificially better by exactly that surplus.",
 "S4-P18": "Selection is where the theorem's directional content becomes testable. The corrected criterion makes two pre-registered predictions. Uncorrected MI-AIC should misclassify toward the candidates with the most missing information about their own parameters. That is the direction of the differential bias. Adding each candidate's own trace should remove the tilt and recover complete-data selection in expectation. @sec-simulations grades both predictions. It also grades the recovery the correction achieves and the residual it leaves. The residual matters. A correctly centered criterion can still select worse than complete-data AIC. The reason is the subject of the rest of this part.",
 "S4-P19": "Selection depends on more than a mean. At the null the limiting law of each anchored deviance is $\\sum_j \\lambda_j \\chi^2_1$ with every weight at least one (@eq-spectrum-sum). So the variance is inflated by $\\sum_j \\lambda_j^2 / q_{\\mathrm{d}}$ even after the mean is fixed. And inflated spread flips rankings. This motivates a graded contrast, pre-registered in full before any code was written. Match the first moment, the first two moments, or the entire null distribution. Then measure what each step achieves. The working statistic anchors every candidate at the saturated model fit to the same imputations.\n$$ \\hat d_k \\;=\\; 2\\big[\\bar Q_\\infty(\\hat\\psi^{*}_{\\text{sat}}) - \\bar Q_\\infty(\\hat\\psi^{*}_k)\\big] \\;\\ge\\; 0. $$\nThe companion derivation writes $T_k$ for this statistic. The deviance notation is used here because the paper's $T$ is the total bias of @eq-split. Anchoring costs nothing, because AIC ranking is invariant to the common shift. And it buys three things. The anchor is the congenial imputation model itself. So the heavy-tailed realized $(A)+(C)$ component and the $O_p(\\sqrt n)$ noise cancel dataset by dataset. This is the @prp-pairing mechanism applied to every candidate. Every $\\hat d_k$ is a proper likelihood-ratio statistic with a constructible per-model null law. The analytic weights $\\lambda_{k,1}, \\dots, \\lambda_{k,q_{d,k}}$ are the nonzero eigenvalues of\n$$ \\big[I_{\\text{com}}^{-1} - G_k(G_k^\\top I_{\\text{com}} G_k)^{-1}G_k^\\top\\big]\\, I_{\\text{com}}\\, I_{\\text{obs}}^{-1}\\, I_{\\text{com}}, \\qquad \\sum_j \\lambda_{k,j} = q_{d,k} + \\operatorname{tr}(\\mathrm{RIV}_{\\perp,k}), \\quad \\lambda_{k,j} \\ge 1, $$\navailable from the law of @prp-lrt without knowing the global truth. And each candidate here is a block-diagonal zero pattern. So the constrained maximizer is closed-form, and\n$$ \\hat d_k \\;=\\; N\\big[\\log|\\hat\\Sigma_k| - \\log|S^{*}|\\big] $$\nexactly, with $S^{*}$ the saturated E-step second-moment matrix.",
 "S4-P20": "Three maps are compared, each built from the analytic null weights. The mean map is the @eq-lrt-bias complement shift.\n$$ \\hat d^{(1)}_k \\;=\\; \\hat d_k - \\operatorname{tr}(\\mathrm{RIV}_{\\perp,k}), $$\nThis is a linear per-model shift that telescopes. So every pairwise comparison is simultaneously mean-corrected. The two-moment map is the affine transformation.\n$$ \\hat d^{(2)}_k \\;=\\; a_k \\hat d_k + b_k, \\qquad a_k = \\sqrt{\\,q_{d,k}\\big/\\textstyle\\sum_j \\lambda_{k,j}^2\\,}, \\qquad b_k = q_{d,k} - a_k \\textstyle\\sum_j \\lambda_{k,j}, $$\nThis is the unique affine map carrying the null law's mean and variance onto those of $\\chi^2_{q_{d,k}}$. Uniqueness is an immediate corollary of the moment identities $\\mathbb{E}[\\sum_j \\lambda_j \\chi^2_1] = \\sum_j \\lambda_j$ and $\\operatorname{Var} = 2\\sum_j \\lambda_j^2$. Matching a misbehaving statistic's first two moments is the standard repair in the structural-equation tradition. The Satorra-Bentler scaled difference statistic matches the mean [@satorraEnsuringPositivenessScaled2010]. The mean-and-variance-adjusted statistics match both from the same moment inputs $\\operatorname{tr}(M)$ and $\\operatorname{tr}(M^2)$ [@asparouhovRobustChiSquare2006]. Here the coefficients are derived from the analytic null law rather than estimated from sandwich matrices. The third map matches all moments by equipercentile equating.\n$$ \\hat d^{(3)}_k \\;=\\; F^{-1}_{\\chi^2_{q_{d,k}}}\\big(F_{W_k}(\\hat d_k)\\big), \\qquad W_k = \\textstyle\\sum_j \\lambda_{k,j} \\chi^2_1, $$\nThe equating function from observed-score test equating is $e_Y(x) = G^{-1}[F(x)]$ [@kolenTestEquatingScaling2014]. We apply it with the analytic null law as $F$ and the complete-data $\\chi^2$ as $G$. By the probability integral transform the equated statistic matches the entire null distribution. The weighted-$\\chi^2$ distribution function is evaluated by numerical inversion of the characteristic function [@imhofComputingDistributionQuadratic1961; @daviesAlgorithm155Distribution1980]. A simulated reference in the style of Chan's Monte Carlo null was the pre-registered fallback. The pre-registration predicted the split the data then delivered. Each step should close more of the null-side gap. The two stronger maps also shrink evidence against misspecified candidates. The affine map shrinks it by $a_k < 1$, and the equating map by approximately $1/\\lambda_{\\max}$ in the far tail. The reason is that no transform built from the observed data can restore destroyed Fisher information.",
 "S4-P21": "The pre-registration states what each map should achieve. The predictions split along the null/noncentral axis. On the null side, each map should close more of the calibration gap than the one before it. Matching the full null distribution should make selection indistinguishable from complete-data AIC wherever overfit flips drive the errors. On the noncentral side, the two stronger maps should shrink the evidence against misspecified candidates by factors computable in advance from the null weights. The affine map shrinks it by $a_k$. The equating map shrinks it by approximately $1/\\lambda_{\\max}$ in the far tail. And where the complete-data benchmark itself struggles, no observed-data correction should close the remaining gap. The shortfall there is information loss rather than miscalibration. All three predictions are reported in @sec-simulations. The report includes the frozen pass criteria they were graded against.",
 "S4-P22": "Two structural limits close this part. Both are proved. First, block-diagonal zero patterns make the naive trace difference and the exact projected trace coincide. Such constraints decompose both information matrices over within-block and cross-block coordinates. This is exactly the equality condition of @prp-naive. The overstatement of @eq-overstate is therefore invisible in block-diagonal candidate families. It is material only for constraints that do not block-decompose, such as the mean restriction in the design of @prp-lrt. Second, per-model marginal transforms cannot calibrate a difference distribution. The dependence between two candidates' scores is invariant under maps applied to each score separately. So no per-model map controls the law of their difference. And near-tied comparisons remain uncalibrated after equating. Selection-aware refinements are developed separately. Both limits are exhibited numerically in @sec-simulations. As in the likelihood-ratio part, everything here is stated for the deterministic-FIML $\\bar Q_\\infty$. The anchoring cancellations are properties of sharing the imputation model. So the construction carries to proper MI unchanged. The proper-MI check is part of the pre-registered evidence in @sec-simulations.",
 "S5-P1": "## Simulation studies: setup\n\nThis section is the paper's complete empirical account. Every quantitative claim in @sec-derivations points here. Every number here traces to one study. Each study fixed its predictions and pass criteria before its code was written (@sec-methods). The setup subsection states each study as a stand-alone design that another analyst could reproduce. The results subsection presents each study as a figure or a table. It includes the predictions that were not met. A reader can enter at any single exhibit. Each exhibit names the perfect reference and the value the study achieved. Everything here carries the *measured* tier of @sec-methods unless stated otherwise.",
 "S5-P2": "**Apparatus.** The designs are multivariate normal in four dimensions, with one bivariate special case. Two imputation arms run throughout. The deterministic arm evaluates the averaged log-likelihood $\\bar Q_\\infty$ in closed form at the full-information maximum-likelihood estimate $\\hat\\theta_{\\text{obs}}$. This arm is the expectation-maximization Q-function with no simulation error. The proper arm draws imputations by expectation-maximization with bootstrapping, as implemented in Amelia [@honakerAmeliaIIProgram2011]. That sampler runs expectation-maximization on bootstrap resamples of the incomplete data. It approximates the posterior of $(\\mu, \\Sigma)$ and then draws completions at those values. The exact alternative is data augmentation. We did not run it. The engine-sensitivity study below bounds what that choice costs.",
 "S5-P3": "**Grading and registration.** Each study is graded against pass criteria fixed before its code existed. The pre-registration documents and their dated amendment histories record those criteria. The bias-decomposition, likelihood-ratio, and selection studies were registered together. The distribution-matching ladder and the non-nested measurement each carry their own registration. The known-scale and sign-regime runs are dedicated single-question designs. Two reporting rules hold throughout. The first rule is that failed predictions appear here in the main text beside the successes. The second rule is that every quantitative claim is stated for totals and paired differentials rather than for components. The first study makes the reason for that second rule concrete.",
 "S5-P4": "**Theorem-validation design.** The data-generating model is a four-variate normal with zero mean and unit variances. Three coordinate pairs have correlations $0.6$, $0.3$, and $0.5$, and the rest are zero. This gives a relative-increase-in-variance trace near $5.55$. The first two coordinates are made missing at random with probabilities $\\Phi(-0.5 + 0.4 X_3)$ and $\\Phi(-0.5 + 0.4 X_4)$. The two fully observed coordinates drive that missingness. The pattern is non-monotone at about one third missing per coordinate. Sample sizes are $N \\in \\{200, 500, 1000, 2000\\}$ with $1000$ repetitions each. The estimand is the net deviance bias $\\mathbb{E}[T]$. It is carried on the analytic arm so it is free of simulation error. The pre-registered target is one half the trace, near $2.77$. We refined it during analysis to the order-one-augmented band $2.42$ to $2.55$ once the missing-at-random term was derived. The study passes if the inverse-variance pooled estimate falls in that band.",
 "S5-P5": "**Known-scale and sign-regime design.** Two single-question runs close the theorem. The known-scale run holds the covariance at its true value and fits only the mean. The conditional missing-data entropy then no longer depends on the estimated parameters. It uses the four-variate normal above at $N = 200$ with $2 \\times 10^5$ repetitions, on both the deterministic and the proper arm. The estimand is again $\\mathbb{E}[T]$. Here @eq-knownscale predicts zero on the deterministic arm and $-\\tfrac12\\operatorname{tr}(\\mathrm{RIV})$ on the proper arm. The sign-regime run imputes from the true model rather than the fitted model. It checks that the total reverses sign, as the fitted-model-versus-true-model distinction of @sec-derivations requires. Each run passes if its arms reproduce the predicted values within Monte Carlo error.",
 "S5-P6": "**Likelihood-ratio design.** The test is $H_0\\!: \\sigma_{12} = 0$ in the four-variate normal. The local alternative is $\\sigma_{12} = \\delta / \\sqrt n$ on the grid $\\delta \\in \\{0, 0.5, 1, 1.5, 2, 2.5, 3, 4\\}$. The sample size is $N = 200$ with $M = 200$ imputations from the proper arm and $1000$ repetitions per grid point. Four references are compared. The complete-data benchmark refers its statistic to $\\chi^2_1$. The corrected and uncorrected numerators are referred to a simulated reference distribution. The Satorra-Bentler arm applies a scaled-and-shifted statistic referred to $\\chi^2_1$. The metric is the rejection rate at level $0.05$. The null arm passes if it sits near nominal, with the corrected numerator slightly conservative as double-counting predicts.",
 "S5-P7": "**Formula-discrimination design.** This run separates the two candidate formulas for the differential bias. The two formulas agree under equal missing information. They diverge when the missing information is uneven. The test is $H_0\\!: \\mu_1 = 0$ in the four-variate normal. The first coordinate is made heavily missing at random through $\\Phi(0.8 + 1.2 X_3)$, about $70$ to $79$ percent. The second coordinate is essentially complete, and the rest are fully observed. One direction is tested. Sample sizes are $N \\in \\{500, 1000\\}$ with $2000$ repetitions on the deterministic arm. The metric is the paired differential bias. The projected trace of @eq-lrt-bias predicts about $2.64$, and the naive trace difference predicts about $8.5$. The run passes if the data land on the projected-trace value.",
 "S5-P8": "**Pairing design.** This run shows that comparing the same imputations under both models collapses the per-dataset noise. The design is the four-variate $\\sigma_{12} = 0$ cell with one tested direction. It runs at $N \\in \\{500, 1000, 2000\\}$ with $2000$ repetitions. Three arms run. The null and the local alternative hold the differential at order one. A fixed alternative is included as the failure mode. The metric contrasts the single-model deviance standard deviation with the paired-differential standard deviation. The run passes if pairing holds the differential standard deviation flat at the null while the single-model figure grows with the sample size.",
 "S5-P9": "**Selection-sweep design.** The candidate set is four nested multivariate-normal covariance models. They range from a diagonal model up to the saturated model. The truth has a compound-symmetry structure. The sweep covers $60$ cells. The cells cross four factors. The first factor is two missingness patterns. The second is the two mechanisms missing at random and not at random. The third is three sample sizes $N \\in \\{200, 500, 1000\\}$. The fourth is five engine slots, which pair the deterministic arm with proper imputation at $M \\in \\{20, 200\\}$ under congenial and uncongenial specification. Missingness is set near $40$ percent, and each cell uses $2000$ repetitions. The metric is the true-model selection rate by the Akaike criterion. It is computed for the complete-data benchmark, the result an analyst would reach with no missing data, the uncorrected imputation criterion, and the corrected criterion. The headline cell is non-monotone missing-at-random on the deterministic arm. The sweep passes on two conditions. The first is that the correction moves selection toward the complete-data benchmark. The second is that the uncorrected criterion's errors concentrate on the largest-missing-information candidate.",
 "S5-P10": "**Distribution-matching ladder design.** This study takes the same candidate family. It contrasts four ways of referring the saturated-anchored imputation deviance to its complete-data law. The contrast runs across a range of signal strength. Three cells run at $N = 500$ with $2000$ repetitions and four tested directions. All three are non-monotone missing at random near $40$ percent on the deterministic arm. The first cell is a main design at correlation $0.40$. The second is a weak-signal design at $0.10$ with near-tied candidates. The third is a junk design at $0$ where the truth is diagonal. Five constructions are compared. They are the complete-data benchmark, the uncorrected deviance, and a mean correction that subtracts the projected trace of @eq-lrt-bias. The remaining two are a two-moment match to the reference and a per-model equipercentile equating. The metric is the true-model selection rate. Null-side distance and variance checks support it, along with the noncentral shrinkage factors. The study passes its pre-registered criteria on three conditions. The first is that the null side calibrates. The second is that the stronger arms reach the complete-data benchmark where overfit flips drive the errors. The third is that the noncentral side shrinks by the predicted factors.",
 "S5-P11": "**Non-nested design.** This measurement isolates the order-one design-imbalance differential of @prp-ac-diff for genuinely separated candidates. That differential is the one quantity the derivations leave unmeasured. The truth is a four-variate first-order autoregressive covariance at correlation $0.5$. Two candidate pairs run. The similar pair sets compound symmetry against the autoregressive model. The dissimilar pair sets a diagonal model against it. Each cell runs at $N \\in \\{500, 1000, 2000\\}$ with $20{,}000$ repetitions. Each cell is also paired with a missing-completely-at-random twin that removes the design-imbalance term. The metric is the isolated differential, the missing-at-random paired residual minus its completely-at-random twin. The run is graded on three separate points. The mechanism passes if the level is order one and collapses under the twin. The dissimilar pair passes if its differential resolves at the predicted scale. The pre-registered similar-pair headline is graded honestly against the resolution the design affords.",
 "S5-P12": "## Simulation studies: results\n\n**Theorem validation.** The net deviance bias tracks the analytic half-trace across every sample size (@fig-w1-theorem). The preregistered target was the leading-order half-trace, near $2.77$. The completed theory of @sec-derivations adds a second term to that target. This term is order one in size. It appears only under missing-at-random data, and it is about $-0.22$. We derived it independently from the missingness mechanism after the run and recorded it as a dated amendment. Adding it shifts the prediction to the band $2.42$ to $2.55$. The inverse-variance pooled estimate is $2.43 \\pm 0.26$. This estimate centers on the augmented band. Its interval also contains the leading-order target $2.77$, which sits $1.3$ standard errors away. The data are therefore consistent with both predictions. One cell is a genuine miss. At $N = 1000$ the estimate falls $2.6$ standard errors below the leading-order target. The cause is the per-repetition statistic, which has heavy tails. Its standard deviation grows with the sample size. At a fixed repetition count the larger cells therefore cannot resolve an order-one offset. The component terms each miss their own targets by roughly $9$ to $20$ percent. For that reason the grading reports only the totals and the paired differentials.\n\n![**Theorem validation.** Each point is the net deviance bias $\\mathbb{E}[T]$ at one sample size with a $95$ percent interval, estimated on the analytic arm with $1000$ repetitions. Perfect is the analytic target, the dotted line at the preregistered half-trace $2.77$ and the shaded band at the order-one-augmented prediction $2.42$ to $2.55$. The solid line is the inverse-variance pooled estimate $2.43 \\pm 0.26$, whose interval contains the leading-order target and centers on the band. The $N = 1000$ cell falls $2.6$ standard errors below the leading-order target, the study's one reported miss.](figures/fig-w1-theorem.png){#fig-w1-theorem width=\"82%\"}",
 "S5-P13": "**Known scale and sign regime.** Holding the covariance known and fitting only the mean collapses the bias to the two values @eq-knownscale predicts (@fig-knownscale). The deterministic arm returns $0.025 \\pm 0.018$ against a target of zero. The proper arm returns $-0.536 \\pm 0.018$ against $-\\tfrac12\\operatorname{tr}(\\mathrm{RIV}) = -0.561$. Both arms land within Monte Carlo error of their targets. The two arms differ by exactly the posterior-draw imputation variance. Imputing from the true model rather than the fitted model reverses the sign of the total. That sign reversal is what the fitted-model-versus-true-model distinction requires.\n\n![**Known scale.** The two imputation arms of the known-scale run at $N = 200$ with $2 \\times 10^5$ repetitions, each with a $95$ percent interval. Perfect is the pair of analytic targets. The target is zero for the deterministic arm and $-\\tfrac12\\operatorname{tr}(\\mathrm{RIV}) = -0.561$ for the proper arm. Both arms reach their target within Monte Carlo error.](figures/fig-knownscale.png){#fig-knownscale width=\"66%\"}",
 "S5-P14": "**Likelihood-ratio absorption.** A correctly calibrated reference absorbs the differential bias at the null, so the test controls its Type I error (@fig-lrt-absorption). The uncorrected numerator rejects at $0.042$ and the additionally corrected numerator at $0.034$. Both sit near the nominal $0.05$. The corrected one is slightly conservative, as double-counting predicts. Raw power across the alternative is not compared here. The four references reject at different Type I error, so a raw power comparison would be confounded. The size-adjusted power arms are the partial support cited for the power conjecture of @sec-discussion. One scope note travels with this study. Its committed correction arm used the naive trace difference. That difference is about $5$ percent from @eq-lrt-bias in this near-invariance design and is immaterial here. Still, @eq-lrt-bias is the correct general form.\n\n![**Likelihood-ratio Type I error.** The rejection rate at the null for four references at $N = 200$ with $1000$ repetitions and Monte Carlo error bars. Perfect is the nominal $0.05$ line. The uncorrected numerator sits at $0.042$ and the corrected one at $0.034$. Both are near nominal. The corrected one is slightly conservative because it double-counts. Raw power across the alternative is not shown. The references reject at different Type I error, so the comparison would be confounded. The size-adjusted power conjecture is treated in the Discussion.](figures/fig-lrt-absorption.png){#fig-lrt-absorption width=\"78%\"}",
 "S5-P15": "**Discrimination of the two formulas.** This run uses an adversarial design that makes one coordinate $70$ to $79$ percent missing. That design separates the two candidate formulas as widely as it allows (@fig-discrimination). The observed differential is $2.64 \\pm 0.11$ at $N = 500$ and $2.70 \\pm 0.11$ at $N = 1000$. Both match the projected-trace prediction of $2.64$. Correctness rests on that match. The same statistic sits $52$ standard errors from the naive trace difference. That gap excludes the naive formula in this engineered cell. That the naive difference always overstates the projected trace is the proved general statement of @prp-naive. The size of the gap here reflects the maximal-separation design rather than typical balanced missingness.\n\n![**Discrimination.** The observed paired differential at two sample sizes with $95$ percent intervals, from $2000$ repetitions on the heavy-missingness $\\mu_1$ design. Perfect is the projected-trace prediction $\\operatorname{tr}(\\mathrm{RIV}_\\perp) = 2.64$, the solid line. The naive trace difference is the dashed line near $8.5$. It is excluded at $52$ standard errors in this maximal-separation cell.](figures/fig-discrimination.png){#fig-discrimination width=\"72%\"}",
 "S5-P16": "**Pairing.** Comparing the same imputations under both models holds the per-dataset noise at order one (@fig-pairing). At the null the paired-differential standard deviation stays near $1.5$ across $N = 500$, $1000$, and $2000$. The single-model figure instead grows from $18.4$ to $36.5$. At a fixed alternative the square-root-of-$n$ growth returns, rising to $12.4$, $16.8$, and $24.9$. This is exactly the failure mode the result specifies.\n\n![**Pairing.** Standard deviation of the deviance statistic against sample size, from $2000$ repetitions, on a logarithmic scale. Perfect is a flat line at order one. The paired differential at the null achieves it, the lower curve. The single-model statistic grows with the sample size. The paired differential at a fixed alternative grows too, the documented limit of pairing.](figures/fig-pairing.png){#fig-pairing width=\"72%\"}",
 "S5-P17": "**Selection.** The uncorrected criterion favors high-missing-information models, and the correction moves selection back toward the complete-data benchmark (@fig-w3-selection). At $N = 500$ on the deterministic arm the complete-data benchmark selects the true model at $0.90$. The uncorrected criterion selects it at $0.67$ and the corrected criterion at $0.82$. In every congenial cell all of the uncorrected criterion's errors land on the saturated, largest-missing-information candidate. This is a registered directional prediction, and it held across all sixty cells. The candidates are nested and ordered by missing information. The saturated model is therefore the natural destination for any downward-biased deviance. So this pattern confirms the predicted direction of the bias rather than discriminating the specific decomposition. The recovery is substantial and visibly short of the complete-data benchmark. That gap is what the distribution-matching ladder was registered to explain.\n\n![**Selection.** True-model selection rate by the Akaike criterion at three sample sizes on the non-monotone missing-at-random cell, $2000$ repetitions. Perfect is the complete-data benchmark, the dashed line and the black bar. The uncorrected criterion sits well below it, and the correction recovers most of the gap. All uncorrected errors fall on the largest-missing-information candidate.](figures/fig-w3-selection.png){#fig-w3-selection width=\"82%\"}",
 "S5-P18": "**The distribution-matching ladder.** Across three levels of signal strength the stronger constructions recover the complete-data benchmark where the errors come from choosing too rich a model. A floor remains where selection is genuinely hard (@fig-ladder). The true-model selection rates are reported below, with Monte Carlo standard error near $0.010$.\n\n| Cell | complete-data | uncorrected | mean | two-moment | equating |\n|---|---|---|---|---|---|\n| $\\rho = 0.40$ | 0.899 | 0.678 | 0.814 | 0.904 | 0.903 |\n| $\\rho = 0.10$ | 0.820 | 0.582 | 0.650 | 0.648 | 0.650 |\n| $\\rho = 0$ (junk) | 0.727 | 0.422 | 0.585 | 0.702 | 0.696 |\n\nAt correlation $0.40$ the two-moment and equating arms reach $0.904$ and $0.903$. The complete-data benchmark is $0.899$. The differences are within twice the Monte Carlo standard error of about $0.010$. So the arms are statistically indistinguishable from the benchmark, with a small overshoot the same interval does not resolve. The junk cell closes $90$ percent of the gap from uncorrected to benchmark. In the weak-signal cell the benchmark itself drops to $0.820$, and no arm passes $0.650$. We read that floor as a limit of the information in the data rather than a calibration error. The calibration checks below support that interpretation but do not separately decompose it in the weak-signal cell. The residual is a measurement on these designs rather than a theorem.\n\n![**Distribution-matching ladder.** True-model selection rate for four constructions in three cells at $N = 500$ with $2000$ repetitions. Perfect is the complete-data benchmark, the dashed line above each cell. At $\\rho = 0.40$ the two-moment and equating arms reach the benchmark. At $\\rho = 0.10$ no arm reaches it, the weak-signal floor.](figures/fig-ladder.png){#fig-ladder width=\"92%\"}",
 "S5-P19": "**Ladder internals.** Two internal measurements show why the ladder works (@fig-ladder-internals). On the true model's anchored statistic the distance to the paired complete-data statistic falls from $0.281$ under the uncorrected deviance to $0.019$ under equating. Over the same arms the variance ratio falls from $3.18$ to near one. The mean correction's paired gap of $0.076 \\pm 0.100$ is a direct confirmation of @eq-lrt-bias at four tested directions. On the underfit candidate the noncentral statistic shrinks by its two predicted factors. The two-moment match shrinks it by the factor $a_k$, predicted near $0.56$ and measured at $0.59$. Equating shrinks it by $1/\\lambda_{\\max}$, predicted near $0.40$ and measured at $0.36$. The internal validity checks held on every repetition. The spectrum-trace identity held to $3 \\times 10^{-15}$, and the reference inversion never failed in $18{,}000$ evaluations.\n\n![**Ladder internals.** Left, the null-side Kolmogorov-Smirnov distance to the complete-data statistic across the four arms in the main cell. Perfect is zero, approached by the two-moment and equating arms, with the variance ratio falling from $3.18$ toward one. Right, the noncentral shrinkage factor measured against predicted on the line $y = x$. The two-moment and equating points sit on the diagonal.](figures/fig-ladder-internals.png){#fig-ladder-internals width=\"100%\"}",
 "S5-P20": "**The structural limits.** Three structural checks of @sec-derivations appear directly in the data. They are the equality case where the naive and projected traces coincide, the cost of the naive moment-map input, and the limit of a per-model map. The table reports them on the selection rates, with Monte Carlo standard error near $0.010$.\n\n| Structural limit | perfect | achieved |\n|---|---|---|\n| block-diagonal family, naive vs projected trace | exact equality | agree to $3.6 \\times 10^{-15}$ |\n| off-equality moment map, main-cell rate | complete-data $0.899$ | naive input $0.824$, correct input $0.904$ |\n| per-model map, equated difference mean and sd | complete-data $10.00$ and $7.10$ | $6.10$ and $5.99$ |\n\nTwo points follow. In this block-diagonal candidate family the naive and projected traces coincide, to $3.6 \\times 10^{-15}$. So the family cannot by itself separate the two trace formulas. The $0.824$ against $0.904$ gap in the second row is therefore a cost of the naive moment-map input, not evidence about the trace formulas. The proof of @prp-naive establishes that the naive trace difference overstates the projected trace whenever the two differ. The non-nested measurement of @fig-w4-nonnested supplies the measured off-equality case, about $9$ percent. The third row is separate again. It shows that a per-model marginal map cannot calibrate the joint difference distribution.",
 "S5-P21": "**The non-nested measurement.** This study checks three claims at once (@fig-w4-nonnested). The mechanism behind the design-imbalance term is confirmed exactly. The predicted scaling resolves for a dissimilar pair. The pre-registered headline for the similar pair stays below resolution. Each candidate's order-one level is large under missing-at-random data, near $+2.6$ and $+2.9$ deviance units for the two candidates. That level collapses under the completely-at-random twin, most sharply for the autoregressive candidate. For the dissimilar diagonal-versus-autoregressive pair the isolated differential resolves at $-1.2$. It sits three and a half standard errors from zero and is stable across sample sizes. For the similar compound-symmetry-versus-autoregressive pair the differential sits below Monte Carlo resolution. A pilot's apparent value of $-2$ dissolved in the full run. We report that failure as the discipline requires. A byproduct supplied the first measured case off the equality condition of @prp-naive. There the naive trace difference overstates the exact projected trace by about $9$ percent.\n\n![**Non-nested measurement.** Left, each candidate's order-one level under missing-at-random data and under its completely-at-random twin, dissimilar pair at $N = 500$ with $20{,}000$ repetitions. Perfect for the twin is zero, and the level collapses toward it, most sharply for the autoregressive candidate. Right, the isolated design-imbalance differential with $95$ percent intervals. Perfect is no effect, the line at zero. The similar pair stays on that line, the reported failure, while the dissimilar pair resolves at $-1.2$.](figures/fig-w4-nonnested.png){#fig-w4-nonnested width=\"100%\"}",
 "S5-P22": "**Engine sensitivity.** The bootstrap imputation sampler reproduces the analytic results on the most demanding cells. This bounds the cost of not running data augmentation. The hardest case is the non-monotone missing-at-random cells with samples as small as $N = 200$, where the bootstrap approximation is weakest. There the sample relative-increase-in-variance trace from the imputation draws matches the observed-data value within $2$ to $3$ percent, with no widening as the sample grows. Selection also agrees between the analytic arm and the imputation arm within $0.011$ on the recovery gap. This robustness is empirical and scoped to the designs studied. The bootstrap sampler is only approximately Bayesian, and data augmentation was not run. A fully Bayesian sampler remains the conservative choice when the fraction of missing information is large (@sec-discussion).",
 "S6-P1": "The result is scoped by its own mechanism. The scope comes first. The bias exists only when the fit estimates a scale or covariance. In that case the conditional missing-data entropy depends on the estimated parameters. A known-scale, location-only fit has no such bias. That is why the estimated-scale clause sits in the theorem rather than in a footnote. Congeniality is assumed throughout. What the bias becomes without it [@mengMultipleImputationInferencesUncongenial1994a] is a separate question. This paper does not take it up. Beyond these scope conditions, six specific limits remain. Each is stated next to the claim it qualifies, with its evidential standing. They are followed by one conjecture, three directions, and the answer to the question the paper opened with.",
 "S6-P2": "**G1.** The absolute magnitude of $(A)+(C)$ is not independently pinned. This qualifies the theorem. The structural facts are firm. Its sign, its $O(1)$ order, its exact MCAR vanishing, the closed form @eq-ac, and the $O(1/n)$ order of the correction beyond the leading-order analytic value are all proved. The absolute magnitude is not. The direct Monte Carlo estimates are heavy-tailed and untrended. The variance-reduced estimate of the higher-order remainder is conditional on the analytic anchor it is paired against. Every comparison in this paper therefore uses $(A)+(C)$ only through the better-conditioned paired differential. **G2.** The proper-MI carry-over of the likelihood-ratio propositions is structural, not separately measured. This qualifies the likelihood-ratio part of @sec-derivations. The propositions are proved and confirmed for the deterministic-FIML $\\bar Q_\\infty$. Under proper MI the imputation-side quantities remain common to both fits. This preserves the cancellation arguments. But no dedicated proper-MI replication of the differential experiments exists.",
 "S6-P3": "**G3.** Imputation-engine robustness is empirical and scoped. This qualifies @sec-simulations. The EMB engine tracks deterministic FIML within the stated tolerances on the designs studied. Exact data augmentation was not run. Nothing here establishes engine-independence beyond those designs. **G4.** The weak-signal floor is a measurement, not an impossibility proof. This qualifies the selection part of @sec-derivations. The residual in the weak-signal cell is information loss as measured through these corrections. No argument here shows that no estimator built from the same observed data could do better.",
 "S6-P4": "**G5.** For dissimilar candidate pairs, a mechanism-independent misspecification $O(1)$ coexists with the design-imbalance $O(1)$. This qualifies @prp-ac-diff and its use in selection. The MCAR contrast separates the two in measurement. But they can reinforce or partially offset each other in the net ranking bias a criterion sees. We make no claim that the design-imbalance term dominates every pair. **G6.** All instantiation is multivariate normal. This qualifies the theorem. The theorem is stated for general regular likelihoods with an estimated scale. But every constant is verified, symbolically and by simulation, only in the normal family.",
 "S6-P4b": "**G6.** The correction's guarantees assume congenial imputation. Uncongeniality is the practical boundary. The imputation model can shrink the cross-block correlations that the analysis estimates. A strong ridge prior does this. When it happens the deviance bias no longer points the way the theory assumes. The sweep includes uncongenial cells, and they show the effect. There the uncorrected criterion already selects the true model more often than the complete-data benchmark, near $0.93$ against the benchmark's $0.91$. The correction then pushes past the benchmark to about $0.98$. That is an overshoot, not a recovery. We registered this behavior as an observational stress test, not as a pass target. It marks where the framework's safety margin ends. The practical recommendation is to diagnose congeniality before trusting bias-corrected selection.",
 "S6-P5": "One question is left as a conjecture. We label it as one. We conjecture that the bias-corrected likelihood-ratio comparison dominates its uncorrected counterpart in power uniformly. The preregistered likelihood-ratio study's power arms provide partial support (@sec-simulations). We state no theorem.",
 "S6-P6": "Three directions seem most worth pursuing. The first is covariate shift. The concluding section of @shimodairaInformationCriterionModel2017 names the combination of a missing mechanism with other sampling mechanisms as future work. The design-imbalance term derived here is nonzero exactly when the missing and observed units differ on the conditioning variables. This is a step into that program. The weighting machinery exists [@shimodairahidetoshiImprovingPredictiveInference2000]. The second is an exact-engine replication. Running the headline studies under data augmentation would convert G3 from an empirical tolerance into a tested equivalence. The third is the calibration program beyond per-model null maps. De-shrinkage of estimated noncentrality and joint calibration of score vectors across candidates are developed separately. So is the extension beyond the normal family.",
 "S6-P7": "The paper closes where it began, with the replication principle. The question was whether an analyst working from multiply imputed data reaches the same conclusions, as often, as the analyst who never lost the data. The answer now has three parts. The first part concerns the criterion itself. The answer is yes. After correction, a deviance or information criterion computed from imputed data means what its complete-data counterpart means, on average, at any signal strength. The second part concerns decisions when the competing models fit alike. The answer is again yes. Corrected selection and calibrated tests behave as they would have with complete data. The third part concerns decisions when one model genuinely fits better. There the answer is no. The missing data carried part of the evidence. Less information means less power. No transformation of the observed data manufactures the lost evidence back. The practical summary for an applied reader is short. Correct the criterion, or match its null distribution. Then trust the result the way complete-data results are trusted. The one exception is where the comparison was close enough that the missing data could have decided it. There, the honest answer is that the data no longer say.",
 "S6-P8": "A final word on the workflow, held to the same standard as everything it produced. Three catches are on the record. The protocol caught a sign error that eight of nine blind re-derivations shipped. It caught a wrong shortcut in an early entropy-gradient argument. And it forced every failed prediction into @sec-simulations. The cost is also visible. Claims arrived slower, hedged to their tier, and two preregistered headlines were given up rather than rescued. What it cannot catch was stated in @sec-methods and bears repeating once. It cannot catch errors shared across model lineages, misreadings of real sources, or designs that answer the wrong question reproducibly. Whether this workflow generalizes beyond one paper is asserted, not demonstrated. What it leaves behind is a public record. The decision log, the preregistrations with their amendments, the verification gates, and the session transcripts are all published. So the assertion can be tested by someone other than its authors."
}
''')

with open(L3, encoding="utf-8") as f:
    l3 = json.load(f)
paras = l3["paragraphs"]
by_id = {p["id"]: p for p in paras}

missing = [pid for pid in REWRITES if pid not in by_id]
assert not missing, f"ids not in level3: {missing}"

changed = 0
for pid, new_prose in REWRITES.items():
    p = by_id[pid]
    if p["draft_prose"] != new_prose:
        p["draft_prose"] = new_prose
        fl = p["review"].get("flags")
        p["review"]["flags"] = (fl + " | " if fl else "") + FLAG
        changed += 1

# guard: the word "oracle" must still appear exactly where it did before (we did not rename it)
ORACLE_PARAS = ["S3-P6", "S4-P4", "S4-P7"]
for pid in ORACLE_PARAS:
    assert "oracle" in by_id[pid]["draft_prose"], f"oracle vanished from {pid}"

l3["metadata"]["status"] = "readability_pass_2026-06-26"
l3["metrics_gate"]["notes"] = (
    "Readability pass 2026-06-26: draft_prose rewritten for the topic/support/wrap arc "
    "and lower grade level across 67 of 82 S1-S6 paragraphs (S3 and the cleanest S1/S2 "
    "paragraphs left as-is). Corpus Flesch-Kincaid grade 11.9 -> 10.2. No number, claim, "
    "hedge, citation, cross-ref, figure markup, or technical term changed; 'oracle' left "
    "verbatim (pending rename). Mechanically verified: math/ref/figure-token multisets "
    "preserved on all 67; prose punctuation gate 0 hits. Prior note: " +
    (l3["metrics_gate"].get("notes") or "")
)

with open(L3, "w", encoding="utf-8") as f:
    json.dump(l3, f, indent=2, ensure_ascii=False)
    f.write("\n")

print(f"readability pass applied: {changed} paragraphs rewritten")
by_sec = {}
for pid in REWRITES:
    by_sec.setdefault(pid.split("-")[0], 0)
    by_sec[pid.split("-")[0]] += 1
print("by section:", dict(sorted(by_sec.items())))
